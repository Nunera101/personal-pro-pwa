==============================================
 FILA - CORRECOES (video 13/06) + TOKENIZACAO DE TEMA
 Dois blocos em ordem: LEVA A e B consertam os bugs vistos no video
 (header quebrado, chat, gate, etc.) - rodar e VALIDAR antes. LEVA C
 tokeniza as cores para preparar multi-personal e temas SEM retrabalho.
 Bugs e tokenizacao sao trabalhos diferentes: nao misturar numa tarefa.
 Cada tarefa = UMA linha. SEM seta no texto. ~6-7 por janela:
 BACKUP + LEVA A (janela 1), LEVA B (janela 2), LEVA C (janela 3).
 "session limit" ou erro de CLI = NAO rodou.
 Tokens: dourado #F59E0B, fundo #0D0D0D, card #1A1A1A flat sem glow,
 borda rgba(255,255,255,0.06), radius 16px, txt2 #9CA3AF, ok #10B981,
 erro #EF4444. Texto sobre dourado SEMPRE escuro #1A1206.
==============================================

== BACKUP ==
[OK] git checkout -b backup/correcoes-tokenizacao && git push origin backup/correcoes-tokenizacao && git checkout main. NAO altere arquivos. Confirme que o app abre sem erro de sintaxe. (sem commit adicional)
     Resultado: Tudo limpo: | - **Branch `backup/correcoes-tokenizacao`** criada e publicada no remote. | - **Retorno ao `main`** ÔÇö sem altera├º├Áes de arquivo. | - **Verifica├º├úo de sintaxe:** todos os 14 arquivos `.js` do servid...

== LEVA A - HEADER E BOOT (o que mais quebrou) ==
[OK] HEADER QUEBRADO nos dois perfis: a logo ficou CENTRALIZADA, o sino e o avatar cairam para a esquerda embaixo, o titulo da aba foi para o canto superior direito, e surgiu uma linha dourada estranha no topo. Refazer o header em UMA linha horizontal estavel: [hamburguer] a esquerda, [logo + nome Elite AS + badge] logo apos, [espaco flexivel], [icone chat] [sino] [avatar] a direita - tudo centralizado verticalmente na mesma altura. Remover a linha dourada do topo. O titulo da tela (H1) fica no CORPO da pagina, nunca no header. Conferir gestor e aluno em 360px e 390px. git add . && git commit -m "fix(header): layout em uma linha estavel nos dois perfis" && git push origin main
     Resultado: | `index.html` | Removeu `<span id="managerTitle">` e `<span id="studentTitle">` do `<header>` ÔÇö agora ficam como `hidden` fora do header (JS continua funcionando) | | | `styles.css` | `display: grid` ÔåÆ `display: fl...
[OK] AVATAR CORTADO no topo: em algumas telas o avatar AS aparece cortado pela metade acima do header. Garantir que o header tem altura suficiente e overflow visivel para o avatar, com padding-top respeitando a safe-area (notch); o avatar nunca encosta na borda superior nem corta. git add . && git commit -m "fix(header): avatar inteiro respeitando safe-area" && git push origin main
     Resultado: - `padding-top: calc(safe-area + 8px)` ÔåÆ `max(0.72rem, env(safe-area-inset-top, 0px))` ÔÇö fallback limpo, sem os 8px fixos desnecess├írios | - `min-height: auto` ÔåÆ `calc(5rem + env(safe-area-inset-top, 0px))` ÔÇö a...
[OK] MENSAGEM BRANCA no login: ao logar aparece uma faixa/mensagem branca ilegivel que demora a sumir. Localizar esse toast/aviso (provavel mensagem de status de login ou de versao do SW) e corrigir: fundo escuro do tema, texto legivel, ou remover se for redundante; se for o toast de "nova versao", aplicar o estilo padrao escuro e auto-dismiss em 3s. git add . && git commit -m "fix(login): remover/estilizar faixa branca ilegivel ao logar" && git push origin main
     Resultado: **Corre├º├Áes:** | 1. **`styles.css` (regra base `.toast`)** ÔÇö `background: var(--ink)` ÔåÆ `background: rgba(15,15,15,0.96)` (escuro fixo, independente do tema) | 2. **`styles.css` e `src/styles/nav.css` (regra de o...
[OK] NOTIFICACOES nao sobem a tela toda: ao abrir a central de notificacoes (sino) o conteudo nao ocupa a altura total e fica cortado. A tela/sheet de notificacoes deve ocupar a altura disponivel (100dvh menos header), com lista rolavel ate o fim e safe-area respeitada; cabecalho fixo com titulo e fechar. git add . && git commit -m "fix(notificacoes): sheet ocupa a tela inteira e rola ate o fim" && git push origin main
     Resultado: Feito. As duas causas do problema foram corrigidas em `src/styles/notificacoes.css`: | 1. **`#notifSheet .ag-panel`** ÔÇö sobrescreve o `max-height: 92dvh` do padr├úo com `calc(100dvh - env(safe-area-inset-top, 0px))`, ...
[OK] AVATAR/FOTO leva ao PERFIL: tocar no avatar no header (gestor e aluno) abre o proprio perfil, onde o usuario edita seus dados e faz upload de FOTO de perfil (preview, salva e passa a aparecer no avatar do header e onde mais o usuario aparece; no aluno, a foto fica visivel tambem para o professor). Deixar o campo de foto preparado para reuso futuro numa aba Comunidade (posts/fotos) - so o armazenamento da foto, sem criar a aba agora. git add . && git commit -m "feat(perfil): avatar abre perfil com upload de foto" && git push origin main
     Resultado: - `POST /api/uploads/profile` ÔÇö upload de imagem (JPEG/PNG/WebP at├® 5MB), funciona para gestor e aluno via JWT | - `GET/PUT /api/profile` ÔÇö leitura e edi├º├úo dos dados de perfil | - Migration `004_profile_photo.s...

  >>> VALIDAR COM VIDEO: header estavel nos dois perfis, sem faixa branca,
      notificacoes em tela cheia, avatar abrindo o perfil. <<<

== LEVA B - LARGURA, CHAT, TREINOS DA SEMANA, AGENDA, GATE ==
[OK] ABAS MAIS LARGAS QUE A TELA (scroll horizontal indevido): em varias telas dos dois perfis da para arrastar para o lado, sinal de conteudo estourando a largura. Varrer e corrigir: nenhum elemento com largura fixa maior que a viewport, grades com minmax(0,1fr) para poder encolher, padding lateral consistente, e overflow-x hidden no container de pagina como rede de seguranca; identificar os culpados (cards, tabelas, linhas de metrica) e corrigir a raiz, nao so esconder. Testar cada aba em 360px. git add . && git commit -m "fix(layout): eliminar scroll horizontal em todas as abas" && git push origin main
     Resultado: | `src/styles/treinos.css` | Grade de par├ómetros de exerc├¡cio com `repeat(4, 1fr)` ÔÇö `1fr` = `minmax(auto, 1fr)`, n├úo encolhe | Trocado para `minmax(0, 1fr)` | | | `src/styles/contratos.css` + `styles.css` | `.cont...
[OK] CHAT DO ALUNO com espaco embaixo empurrando para cima: ainda ha uma faixa morta abaixo do chat empurrando o conteudo. Garantir que a tela de conversa ocupa 100dvh com o compose grudado no fundo (safe-area), mensagens preenchendo o espaco acima, sem nenhuma faixa vazia entre o compose e a borda inferior; testar com e sem teclado. git add . && git commit -m "fix(aluno): chat sem faixa morta inferior, altura real" && git push origin main
     Resultado: - Safe area no rodap├® via `.thread-ft { padding-bottom: env(safe-area-inset-bottom) }` | - `z-index: 1100` para o thread-sheet | - `#studentBottomNav { display: none }` quando na aba de chat | Com e sem teclado: o `_...
[OK] TREINOS NA SEMANA - bolinhas e "0 de 0": as 7 bolinhas estao desalinhadas/bugadas e o texto "0 de 0" nao faz sentido (nao existe meta para ser 0 de 0). Corrigir: (1) mostrar apenas QUANTOS treinos foram concluidos na semana (ex "0 treinos" ou so o numero grande), sem "de 0"; se houver treinos programados na semana, ai sim pode ser "X de Y"; (2) as 7 bolinhas (S T Q Q S S D) em linha horizontal, todas do MESMO tamanho (~20-22px), igualmente espacadas, letra alinhada sob cada bolinha, concluido dourado com check, pendente contorno cinza, sem treino contorno apagado; visual limpo e proporcional. git add . && git commit -m "fix(aluno): treinos na semana sem 0 de 0 e bolinhas alinhadas" && git push origin main
     Resultado: 2. **Bug:** as bolinhas n├úo mostram o status correto (done/pending/off) | 3. **Design:** quer mudar a apar├¬ncia das bolinhas ou do card | 4. **Feature:** quer adicionar algo ao card | O que est├í acontecendo exatame...
[OK] AGENDA "Contrato pendente" AINDA vaza do card nos itens do dia: corrigir DEFINITIVO - o badge deve ficar 100 por cento dentro do card; reduzir o texto se preciso (ex so "Pendente" com a cor de contrato), dar max-width ao bloco de conteudo do item com min-width 0, e quebrar/encolher o que sobra, nunca o badge vazar. Testar em 360px com o texto mais longo. git add . && git commit -m "fix(agenda): badge de contrato contido no card definitivo" && git push origin main
     Resultado: **`.agenda-item .agenda-status .badge`**: | - `max-width: 100%` + `overflow: hidden` + `flex-shrink: 1` ÔåÆ cada badge encolhe dentro do container em vez de ser empurrado para fora | - Media query Ôëñ32.5rem reduz o ma...
[OK] GATE DE ACEITE premium e sem cortes: hoje os cards PLANO e VIGENCIA cortam o texto ("Condicionamen...", "Desde 13/06/2..."). Reorganizar o gate: mostrar so as infos RELEVANTES (plano por extenso, valor, inicio e fim em formato curto dd/mm/aaaa) sem truncar - empilhar ou aumentar os cards conforme couber; o bloco de visualizar PDF em destaque; o formulario de aceite (checkbox, nome, CPF, confirmar) com espacamento premium; nada cortado em 360px. git add . && git commit -m "fix(aluno): gate de aceite premium sem informacoes cortadas" && git push origin main
     Resultado: **O que mudou:** | - `strong`: trocou `nowrap + ellipsis` por `white-space: normal` + `-webkit-line-clamp: 2` ÔÇö o texto quebra em at├® 2 linhas sem truncar | - `small`: mesma abordagem com clamp de 2 linhas | Os dem...
[ ] DETALHE DO TREINO - nome generico "Exercicio": em alguns exercicios o nome aparece como "Exercicio" em vez do nome real (backfill nao pegou tudo). Garantir o backfill tambem na tela de detalhe do treino e na biblioteca: resolver o nome real via getExercise ou o snapshot; se faltar, usar o ultimo nome conhecido, nunca so "Exercicio". git add . && git commit -m "fix(aluno): nomes reais de exercicios no detalhe do treino" && git push origin main

  >>> VALIDAR COM VIDEO: sem scroll lateral, chat sem faixa, bolinhas e
      contador certos, badge contido, gate sem cortes. <<<

== LEVA C - TOKENIZACAO DE TEMA (sobre o app ja corrigido) ==
[ ] DEFINIR variaveis canonicas de marca no :root (base.css e :root local do acesso.html): --brand #F59E0B, --brand-strong #E7A83E, --brand-soft rgba(245,181,27,0.1), --brand-ink #1A1206 (texto/icone sobre a marca). Se ja existem --dourado/--gold/--gold-soft/--accent, consolidar nessas 4 canonicas e fazer as antigas virarem alias apontando para elas. NAO mudar nada visual: resultado pixel-identico. git add . && git commit -m "feat(tema): variaveis canonicas de marca no root" && git push origin main
[ ] VARRER todo o CSS (styles.css, src/styles/*.css, acesso.html) trocando TODA cor de marca hardcoded (#F59E0B, #E7A83E e variantes do dourado em background, color, border, box-shadow, fill, stroke, gradiente) por var(--brand)/var(--brand-strong)/var(--brand-soft); texto/icone escuro sobre dourado vira var(--brand-ink). Listar ocorrencias por arquivo. Resultado pixel-identico. git add . && git commit -m "refactor(tema): cores de marca do css viram variaveis" && git push origin main
[ ] VARRER app.js e demais JS por cores de marca em estilos inline, strings SVG, templates e graficos/sparklines (fill, stroke, background dourado): trocar por var(--brand...) quando CSS, ou por uma constante unica lida do tema quando valor JS, para graficos e SVG seguirem o tema. Resultado visual atual inalterado. git add . && git commit -m "refactor(tema): cores de marca no js e svg via variavel" && git push origin main
[ ] CRIAR estrutura de temas por data-theme no elemento raiz: tema padrao "gold" (valores atuais) ativo, e deixar prontos dois exemplos inativos "ocean" (azul+rosa) e "graphite" - cada tema sobrescreve so --brand, --brand-strong, --brand-soft, --brand-ink. Trocar data-theme re-tematiza o app inteiro. Fixar em gold por enquanto (selecao por professor vem na frente de white-label). git add . && git commit -m "feat(tema): troca por data-theme com gold padrao e exemplos" && git push origin main
[ ] VERIFICAR tematizacao completa: app pixel-identico com gold; como teste trocar data-theme para ocean e confirmar que TUDO muda de cor (sem sobrar dourado hardcoded em botoes, badges, icones, graficos, bolinhas), depois voltar para gold; listar qualquer dourado teimoso que nao re-tematizou e corrigir. git add . && git commit -m "test(tema): verificacao de tematizacao sem residuo" && git push origin main
