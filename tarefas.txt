== BACKUP (OBRIGATORIO) ==
[OK] git checkout -b backup/seguranca-profunda && git push origin backup/seguranca-profunda && git checkout main. NAO altere arquivos. Confirme que o app abre e que o login de gestor e de aluno funcionam ANTES de comecar. (sem commit adicional)
     Resultado: Credenciais de refer├¬ncia que descobri nos dados de seed, caso queira testar manualmente no navegador: | - **Gestor:** `admin@personalpro.app` / `Admin@2026` | - **Aluno:** `joao@teste.com`, `maria@teste.com` ou `carl...
     Resultado: There's an issue with the selected model (claude-fable-5). It may not exist or you may not have access to it. Run --model to pick a different model.


== LEVA S1 - CREDENCIAIS E EXPOSICAO (critico, rapido) ==
[ ] REMOVER credenciais expostas do index.html: apagar o bloco que mostra e-mail e senha do admin em texto puro no HTML publico (RELATORIO-SEGURANCA C1). Garantir que nenhuma credencial, senha ou hash apareca em arquivos servidos ao cliente (index.html, acesso.html, qualquer .js do front). Listar o que foi removido. git add . && git commit -m "fix(seguranca): remover credenciais expostas do html publico" && git push origin main
     Resultado: There's an issue with the selected model (claude-fable-5). It may not exist or you may not have access to it. Run --model to pick a different model.

[ ] SENHA DO ADMIN via ambiente, sem hardcode: remover a senha hardcoded "Admin@2026" do server (RELATORIO-SEGURANCA C2, api.js:517). O login do admin passa a validar contra ADMIN_EMAIL e ADMIN_PASSWORD_HASH lidos de variavel de ambiente (process.env); se as variaveis nao existirem em ambiente de desenvolvimento, usar um fallback APENAS local que imprime um aviso no console e NUNCA vai para o cliente. Documentar no README/comentario quais variaveis definir no Railway (ADMIN_EMAIL, ADMIN_PASSWORD_HASH) e deixar um script ou instrucao para gerar o hash bcrypt da senha. NAO commitar nenhuma senha real. Testar: login do admin funciona com a credencial de ambiente. git add . && git commit -m "fix(seguranca): senha do admin via env, sem hardcode" && git push origin main
     Resultado: There's an issue with the selected model (claude-fable-5). It may not exist or you may not have access to it. Run --model to pick a different model.

[ ] JWT_SECRET obrigatorio e estavel: remover o fallback hardcoded do segredo JWT (config.js:18, "personal-pro-local-dev-secret") e o fallback aleatorio por restart (auth.js:6-9) que invalida sessoes a cada deploy; exigir JWT_SECRET de process.env, com erro claro de inicializacao se ausente em producao e um fallback fixo only-dev avisado no console. Documentar a variavel. Testar: app sobe e login persiste entre restarts. git add . && git commit -m "fix(seguranca): JWT_SECRET obrigatorio via env" && git push origin main
     Resultado: There's an issue with the selected model (claude-fable-5). It may not exist or you may not have access to it. Run --model to pick a different model.

[ ] FECHAR /api/health e segredos: remover da resposta publica de /api/health os campos sensiveis (status do banco, storage, mail, push) - retornar apenas { ok: true } para o publico; se precisar de health detalhado, criar /api/health/detail protegido por requireManager. git add . && git commit -m "fix(seguranca): health endpoint sem vazar infraestrutura" && git push origin main
     Resultado: There's an issue with the selected model (claude-fable-5). It may not exist or you may not have access to it. Run --model to pick a different model.

