==============================================
 FILA DE TAREFAS - Personal Pro PWA
 RODADA: Padronizacao Global + Abas + Inovacoes
 Formato: [ ] descricao da tarefa
 Nao altere linhas com [OK] ou [ERRO]
==============================================

[OK] BACKUP DE SEGURANCA. Antes de tudo crie um ponto de retorno: git checkout -b backup-padronizacao && git push origin backup-padronizacao && git checkout main. Confirme com git branch -a. Verifique que o app abre sem erro de sintaxe antes de prosseguir.
     Resultado: - **Branch `backup-padronizacao`** criado e publicado em `origin` com sucesso. | - **Branches ativos:** `main` (current), `backup-padronizacao`, `backup-antes-refatoracao`, `backup-polish` ÔÇö todos no remote. | - **Si...

[OK] CORRETIVO GLOBAL 1 - BARRA INFERIOR FIXA (afeta todas as telas). A barra de navegacao inferior nao fica fixa: so aparece ao rolar ate o fim do conteudo. Causa provavel: as animacoes de transicao de view (classe .is-entering ou similar com transform/translateY) aplicam transform num container ANCESTRAL da barra; quando um ancestral tem transform/filter/will-change, o position:fixed do filho passa a se referir a esse ancestral, quebrando o comportamento fixo. Corrija: 1) garanta que a barra inferior NAO esteja dentro de container que recebe transform durante animacoes; se estiver, mova-a para fora, como filha direta do body, irma do container de conteudo. 2) aplique na barra position:fixed; left:0; right:0; bottom:0; z-index:1000; padding-bottom:env(safe-area-inset-bottom,8px). 3) remova transform/filter/will-change de ancestrais da barra (anime apenas o conteudo interno das telas, nunca o wrapper que contem a navbar). 4) o conteudo das telas deve ter padding-bottom de calc(80px + env(safe-area-inset-bottom)) para o ultimo item nao ficar atras da barra. Teste: ao rolar qualquer tela a barra permanece colada na base. Depois git add . && git commit -m "corrige barra inferior fixa em todas as telas" && git push origin main.
     Resultado: **Causa raiz:** Os `#managerBottomNav` e `#studentBottomNav` estavam dentro das `<section class="view">`. A anima├º├úo `.view.is-entering` aplica `transform: translateY(4px)` no ancestral ÔÇö isso faz o browser criar um ...

[OK] CORRETIVO GLOBAL 2 - REMOVER TODO GLOW/BLUR (afeta todas as telas). Ainda ha brilho/glow amarelo desfocado nas bordas de cards e halo ao redor dos botoes dourados (ex: Agendar atividade, Novo padrao, Novo plano alimentar). Faca varredura final em styles.css e src/styles/ removendo: box-shadows coloridos/difusos dourados, radial-gradients de brilho em pseudo-elementos ::before/::after de cards e botoes, backdrop-filter, e filter:blur remanescentes. Resultado: cards e botoes totalmente chapados, fundo solido #1A1A1A, borda fina rgba(255,255,255,0.06), botoes dourados sem halo. Mantenha apenas sombras discretas escuras rgba(0,0,0,0.3) se necessario. Depois git add . && git commit -m "remove todo glow e liquid glass do app" && git push origin main.
     Resultado: - Todos os `radial-gradient` com cores douradas em backgrounds de cards/bot├Áes | - Todos os `backdrop-filter: blur(...)` e `filter: blur(...)` | - Box-shadows compostos (ex: `inset -2px + glow dourado`) ÔåÆ apenas a p...

[OK] CORRETIVO GLOBAL 3 - CARD CORTADO EM FILEIRAS DE 3 (afeta Dashboard, Padroes, Atualizacoes e qualquer faixa de 3 metricas). O 3o card de fileiras com 3 metricas fica cortado na direita e exige scroll lateral. Causa: faixas usam overflow-x:auto com min-width nos cards. Corrija: TODA faixa de exatamente 3 cards de metrica deve usar grade de 3 colunas iguais que sempre cabem: display:grid; grid-template-columns:repeat(3,1fr); gap:10px; sem overflow-x e sem min-width. Reduza padding interno e tamanho de fonte do card se necessario para caber em telas de 360-390px sem corte. Faixas de 2 cards usam repeat(2,1fr); faixas de 4+ cards podem manter scroll horizontal. Aplique essa correcao em Dashboard, Padroes (Total/Rascunhos/Publicados), Atualizacoes (Recebidas/Pendentes/Atrasadas) e qualquer outra faixa de 3. Depois git add . && git commit -m "corrige 3o card cortado usando grade de 3 colunas" && git push origin main.
     Resultado: **`src/styles/base.css`** ÔÇö adicionadas regras compactas para `.metrics-row--3 .metric-card`: | - `padding: 10px` (era 14px) | - `font-size` reduzido no label (10px) e valor (`clamp(18px, 5vw, 26px)`) | - Garante qu...

[OK] CORRETIVO GLOBAL 4 - ENCODING RESIDUAL E ELITE AS. Corrija o encoding residual que persiste em textos dinamicos em varias telas: o traco "–" aparece como â€"; o bullet "•" aparece como â€¢; a seta "↑"/"↓" pode aparecer como â— ou similar; "Ãšltima" deve ser "Última"; "Ultimo" deve ser "Último". Garanta que fixMojibake cubra todos esses pares e seja aplicado nos textos dinamicos de todas as telas. Alem disso, remova qualquer eyebrow/overline "ELITE AS" que ainda apareca acima de algum titulo de tela (o H1 deve ser sempre o primeiro elemento). Depois git add . && git commit -m "corrige encoding residual de traco bullet seta e remove eyebrow ELITE AS" && git push origin main.
     Resultado: +   & "$basedir/node_modules/@anthropic-ai/claude-code/bin/claude.exe"  ... | +   ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ |     + CategoryInfo          : NotSpecified: (error: unknown option...

[OK] CORRETIVO GLOBAL 5 - QUEBRA DE TEXTO E BADGE ESTOURANDO (auditoria + correcao em todos os cards). Em varios cards o texto longo causa reticencias feias ou empurra o badge de status para fora da borda (ex: objetivo do aluno invadindo o badge; Contrato pendente pulando para fora na agenda; Contrato value cortado). Padronize o comportamento em TODOS os cards de lista do app (alunos, agenda, contratos, dieta, padroes, atualizacoes, mensagens): o container do item deve ser flex com o bloco de texto usando flex:1 e min-width:0 (permitindo truncar com reticencias de forma limpa) e o badge de status com flex-shrink:0 (nunca encolhe nem e empurrado para fora da borda). Textos secundarios longos truncam com white-space:nowrap; overflow:hidden; text-overflow:ellipsis. Nenhum badge pode vazar do card. Gere ao final um arquivo AUDITORIA-QUEBRAS.md listando cada tela onde havia quebra/corte e o que foi ajustado. Depois git add . && git commit -m "corrige quebra de texto e badge estourando em todos os cards" && git push origin main.
     Resultado: | `contratos.css` | 4 seletores sem overflow: `span` e `b` do info do aluno + `small` e `strong` da validade ÔÇö corrige "Contrato value cortado" | | | `dieta.css` | 5 seletores: t├¡tulo do plano, objetivo (icon + texto...

[OK] ABA PADROES - ADEQUAR AO DESIGN SYSTEM (esta completamente fora do padrao). Analise renderManagerWorkouts, renderPatternCard, patternMetricCard, patternMetaItem, renderPatternExercisePreview, patternFilterSelect. Correcoes: 1) Subtitle curto "Modelos base reutilizaveis". 2) Botao "+ Novo padrao" full-width dourado proporcional (padding vertical ~14px), sem halo. 3) As 3 metricas (Total de padroes, Rascunhos, Publicados) em grade de 3 colunas (corretivo global 3). 4) Substituir os 3 dropdowns empilhados (Status, Objetivo, Nivel) por campo de busca flex:1 + UM botao Filtrar compacto na mesma linha, com as opcoes dentro do painel do filtro. 5) Remover o cabecalho redundante "Padroes cadastrados" + badge "X padrao(s)" + descricao. 6) CARD DE PADRAO - reestruturacao total: hoje cada info (Objetivo, Nivel, Exercicios, Ultima edicao) esta numa CAIXA GRANDE SEPARADA empilhada, deixando o card gigante. Mude para: topo com icone (haltere em quadrado dourado) + nome bold + badge de status (Publicado=verde, Rascunho=azul); abaixo as 4 infos em grade 2x2 COMPACTA SEM caixas separadas (apenas icone pequeno + label 11px cinza + valor branco); usar icone de ALVO para Objetivo (nao grafico de barras); preview de exercicio em 1 linha truncada; base com botao "Aplicar" + botao "..." (icone, nao "Mais acoes"). Card chapado, altura proporcional. 7) Corrigir "Ãšltima edicao" para "Última edicao". 8) Estado vazio padrao se nao houver padroes. Depois git add . && git commit -m "adequa aba Padroes ao design system com card compacto e filtro inline" && git push origin main.
     Resultado: 1. **Subtitle** ÔÇö `"Monte modelos base reutiliz├íveis com exerc├¡cios da biblioteca."` ÔåÆ `"Modelos"` | 2. **`section-title` count** ÔÇö `<span class="badge">` ÔåÆ `<span class="small-text">X padr├úo(s) encontrado(s)...

[OK] ABA PERFIL DO ALUNO - FINALIZAR ADEQUACAO. Analise renderStudentProfileHero, renderStudentSummaryCards, profileSummaryCard, renderProfileActivityCard. Correcoes restantes: 1) HERO CARD compacto: avatar a esquerda + nome bold + "Objetivo: X" + badge de status, com botao "..." no canto SUPERIOR DIREITO alinhado ao topo (nao flutuando no meio); o menu que o "..." abre NAO pode ser cortado (garanta que o card e ancestrais nao tenham overflow:hidden que corte o menu, ou renderize o menu como overlay com z-index alto). 2) OS 6 CARDS DE METRICA de 2x3 para GRADE 3 COLUNAS x 2 LINHAS (Acesso, Contrato, Ultima atualizacao na linha 1; Proximo treino, Treinos na semana, Volume recente na linha 2). Dentro de cada card: icone maior a esquerda + label e valor a direita. Os labels NAO podem truncar ("ULTIMA ATUALIZA...", "TREINOS NA SEMA...") - reduza a fonte do label ou ajuste para caber, ou quebre em duas linhas de forma limpa. Cards compactos e chapados. 3) Na Visao geral, os cards "Proximo treino" e "Ultimo treino concluido" lado a lado em duas colunas, layout clean. Depois git add . && git commit -m "finaliza Perfil do aluno: 6 cards em 3x2, hero com menu no canto, treinos lado a lado" && git push origin main.
     Resultado: Duas corre├º├Áes aplicadas e publicadas: | 1. **Labels dos 6 cards de m├®trica n├úo truncam mais** ÔÇö `.profile-card-body > span` mudou de `white-space: nowrap + text-overflow: ellipsis` para `white-space: normal + -we...

[OK] ABA DIETA - FILTROS INLINE E LIMPEZA. Analise renderManagerDiet, dietFilterSelect, renderDietPlanCard. Correcoes: 1) Manter botao "+ Novo plano alimentar" full-width com icone de maca (conforme ref), sem halo. 2) As 3 metricas (Planos ativos, Revisoes pendentes, Proximas entregas) em grade de 3 colunas. 3) Substituir os filtros antigos grandes (dois dropdowns "Filtrar Todos" + "Objetivo Todos" + lupa flutuante solta) por campo de busca flex:1 + UM botao Filtrar compacto na mesma linha, com Objetivo dentro do painel do filtro. 4) Card de plano: avatar + nome + objetivo + badge status; linha com Protocolo/kcal e Refeicoes/dia; Ultima atualizacao e Proxima revisao; botoes Abrir plano / Enviar link. Card compacto e chapado. 5) Estado vazio padrao. Depois git add . && git commit -m "adequa aba Dieta: filtros inline e cards compactos" && git push origin main.
     Resultado: Pode completar as corre├º├Áes que precisa? Por exemplo: | - O que exatamente deve mudar nos filtros (inline significa juntar em uma linha? remover algum filtro?) | - O que deve ser limpo ou removido? | - O que mais al...

[OK] ABA CONTRATOS - FILTROS INLINE E CARDS RICOS. Analise renderManagerContracts, contractFilterSelect, renderContractCard. Correcoes: 1) As 3 metricas (Pendentes, Assinados, Proximos vencimentos) em grade de 3 colunas. 2) Substituir os 3 dropdowns empilhados (Filtrar contratos, Aluno, Plano) por UM botao Filtrar compacto que abre um painel com as 3 opcoes dentro (ou no maximo 1 dropdown visivel + botao de filtros). 3) Card de contrato: avatar + nome do aluno + plano (ex: Plano Elite) + valor mensal em dourado (ex: R$ 299,90/mes); a direita data "Valido ate DD/MM/AAAA" + badge de status; botao contextual na base (Visualizar/Reenviar/Gerar PDF conforme status). Remover o "..." solto no topo do card atual. 4) Card "Novo contrato" com borda tracejada dourada + "+". 5) Estado vazio padrao. Card chapado. Depois git add . && git commit -m "adequa aba Contratos: filtros inline e cards completos" && git push origin main.
     Resultado: - Os 3 selects dentro em `.contract-filter-grid` | - O painel mant├®m aberto automaticamente ap├│s qualquer sele├º├úo de filtro (`state.contractFilterOpen`) | **3. Valor no card com `/m├¬s`** | `contract.value` agora ...

[OK] ABA FINANCEIRO - CORRIGIR BUGS VISUAIS E PADRONIZAR. Analise renderManagerFinance, financeFilterSelect, renderFinanceRecordCard, renderFinanceChart. Correcoes: 1) Botao "Registrar pagamento" compacto no canto superior direito (.btn-action-header), nao full-width. 2) As 4 metricas (Recebido no mes, A receber, Em atraso, Alunos pagantes) em faixa horizontal limpa - se nao couberem 4 em grade, use scroll horizontal suave com cards de min-width consistente, sem corte feio do ultimo. 3) Corrigir a busca e filtro baguncados (busca cortada "Bu..." + dropdown "Todos" solto + Filtrar): unifique em campo de busca flex:1 + UM botao Filtrar compacto na mesma linha. 4) Secao "Visao do mes" com seletor de mes (< junho 2026 >) + grafico de linha + KPIs (Faturamento, Ticket medio, Inadimplencia) em coluna a direita, sem sobreposicao. 5) Lista de mensalidades: avatar + nome + plano + valor/mes + vencimento + badge status + botao contextual (Visualizar/Cobrar/Recibo). Cards chapados, sem glow, sem quebras. Depois git add . && git commit -m "corrige bugs visuais do Financeiro e padroniza ao design system" && git push origin main.
     Resultado: 3. **Busca + filtro unificados**: removido o `financeFilterSelect` monol├¡tico (que roubava espa├ºo e cortava a busca em "Bu..."). Substitu├¡do por um `filter-btn` compacto ao lado da busca, com painel colaps├ível `finan...

[OK] ABA MENSAGENS - TRANSFORMAR EM LAYOUT ESTILO WHATSAPP (ousado e funcional). Analise renderManagerMessages, buildMessageConversations, renderConversationCard, renderConversation, openMessages, handleMessageForm. Reformule completamente a tela para um layout de mensageria moderno estilo WhatsApp: 1) REMOVER os cards de metrica (Mensagens nao lidas / Conversas ativas) do topo - no maximo um contador discreto. 2) TELA DE LISTA DE CONVERSAS: campo de busca no topo para filtrar por nome do aluno; abaixo, uma lista vertical de conversas onde cada linha tem: avatar circular do aluno a esquerda + nome do aluno em branco bold + previa da ultima mensagem em cinza truncada em 1 linha + horario da ultima mensagem no canto superior direito + badge dourado circular com numero de mensagens nao lidas (quando houver). Linhas separadas por divisor sutil, toda a linha clicavel. Ordenar por mensagem mais recente. 3) TELA DE CONVERSA ABERTA (ao tocar numa conversa): header com seta de voltar + avatar + nome do aluno; corpo com bolhas de mensagem estilo chat (mensagens do aluno alinhadas a esquerda com fundo escuro, mensagens do gestor alinhadas a direita com fundo dourado/escuro destacado, cada bolha com horario pequeno); barra de input fixa na base com campo de texto + botao de enviar. 4) Estado vazio: quando nao houver conversas, mostrar empty-state "Nenhuma conversa ainda / Abra o perfil de um aluno para iniciar". 5) Tudo chapado, sem glow, respeitando areas seguras e a barra inferior fixa. Seja caprichoso para parecer um app de mensagens real e organizado. Depois git add . && git commit -m "transforma aba Mensagens em layout estilo WhatsApp com lista de conversas e chat" && git push origin main.
     Resultado: - Bolhas WhatsApp: aluno ├á esquerda (fundo escuro), gestor ├á direita (fundo dourado), hor├írio pequeno em cada bolha | - Scroll autom├ítico para a ├║ltima mensagem ao abrir | - Compose bar fixa na base: textarea que ...

[OK] REVISAO GLOBAL DE ICONES CONFORME AS REFERENCIAS. Faca uma revisao de todos os icones do app para coerencia e fidelidade. Padronize um conjunto consistente (todos no mesmo estilo de linha, mesma espessura, dourado quando ativo). Mapeie corretamente: Dashboard=grade 4 quadrados; Alunos=pessoa; Agenda=calendario; Padroes=camadas empilhadas; Mais=tres pontos; objetivo do aluno=alvo; treino=haltere; contrato=documento; mensagens=balao de chat; financeiro=carteira/cifrao; dieta=maca/talher; atualizacao=relogio ou camera; acesso=pessoa/login; volume=grafico de barras. Substitua qualquer icone generico ou divergente das referencias. Gere ICONES-REVISAO.md listando os icones trocados por tela. Depois git add . && git commit -m "revisa e padroniza icones de todo o app conforme referencias" && git push origin main.
     Resultado: - `finance`: cart├úo de cr├®dito ÔåÆ **cifr├úo** (`$`) ÔÇö refer├¬ncia pedia carteira/cifr├úo | - `updates`: documento com lupa ÔåÆ **c├ómera** ÔÇö refer├¬ncia pedia rel├│gio ou c├ómera; c├ómera escolhida por `today` j├...

[OK] RELATORIO PARCIAL DE CONSISTENCIA. Percorra todas as telas (primarias e secundarias) e gere RELATORIO-PADRONIZACAO.md confirmando que nao ha mais 3o card cortado, badge estourando, glow, encoding residual ou barra inferior solta em nenhuma tela, e listando qualquer ponto que ainda divirja das referencias. Este relatorio marca o fim da fase de padronizacao e o inicio da fase de inovacao abaixo. Depois git add . && git commit -m "relatorio de consistencia pos padronizacao" && git push origin main.
     Resultado: - Strings `"├óÔé¼"` no source de `app.js` (corrigidas em runtime, s├│ higiene) | - Linear-gradient muito sutil nos cards de Contratos e Finan├ºas (sheen branco, n├úo dourado) | - Compose-bar do chat sem teste em dispos...

[OK] INOVACAO 1 - SPLASH E SKELETON SCREENS (acabar com a tela preta longa). Hoje o app mostra uma tela preta por muito tempo ao abrir. Transforme isso em uma experiencia premium: 1) SPLASH: ao abrir, mostrar imediatamente um splash com a logo do Elite AS centralizada em fundo escuro (com uma animacao sutil de fade ou pulso), enquanto a sessao e verificada. Tempo maximo curto antes de decidir o destino (app ou login). 2) SKELETON SCREENS: quando uma tela com lista (Alunos, Dieta, Contratos, Mensagens, Agenda, Padroes) esta carregando dados, em vez de tela vazia ou preta, mostrar placeholders cinza no formato dos cards (retangulos com leve animacao shimmer pulsante) que somem quando o conteudo real chega. Crie uma classe reutilizavel .skeleton com animacao de shimmer (gradiente cinza animado) e componentes skeleton para card de lista e para metricas. Isso da sensacao de velocidade mesmo durante o carregamento. Respeite prefers-reduced-motion. Depois git add . && git commit -m "adiciona splash com logo e skeleton screens para carregamento premium" && git push origin main.
     Resultado: - **`prefers-reduced-motion`** ÔÇö coberto pelo bloco global j├í existente no CSS. | - **Helpers de layout:** `.skeleton-card`, `.skeleton-card--tall`, `.skeleton-metric`, `.skeleton-grid-2`, `.skeleton-grid-4`. | - **...

[OK] INOVACAO 2 - NOTIFICACOES PUSH (Web Push API). O app e uma PWA com service worker, entao tem a base para notificacoes push reais que aparecem no celular mesmo com o app fechado. Implemente: 1) Gerar par de chaves VAPID e configurar no backend (server/) o envio de Web Push. 2) No cliente, apos o login, pedir permissao de notificacao de forma elegante (nao no primeiro segundo - peca quando fizer sentido, ex: apos a primeira interacao) e registrar a subscription via pushManager.subscribe(), salvando no backend associada ao usuario. 3) No service worker (sw.js), tratar o evento push para exibir a notificacao (titulo, corpo, icone do app) e o evento notificationclick para abrir o app na tela relevante. 4) Disparar notificacoes nos eventos chave: aluno recebe notificacao quando o gestor publica um treino novo, responde uma mensagem, ou marca/atualiza uma atividade na agenda dele; gestor recebe quando um aluno envia atualizacao de progresso ou mensagem. 5) Tratar com seguranca: se o navegador nao suportar ou o usuario negar, o app continua funcionando normalmente sem erros. NAO faca nada destrutivo no backend - apenas adicione o necessario para push. Documente em PUSH-SETUP.md as chaves e variaveis de ambiente necessarias (sem expor segredos). Depois git add . && git commit -m "adiciona notificacoes push via Web Push API para treinos mensagens e agenda" && git push origin main.
     Resultado: - `requestPushPermission()` ÔÇö exibe banner fixo na base da tela, aguarda aceitar/recusar, s├│ chama `Notification.requestPermission()` se aceitar | - `registerPushSubscription()` ÔÇö usa `pushManager.subscribe()` com ...

[OK] INOVACAO 3 - MICROINTERACOES COM PROPOSITO (sensacao de conquista). Adicione microinteracoes que reforcam progresso e dao feedback claro, sem exagero: 1) EXECUCAO DE TREINO: ao concluir uma serie, animacao sutil de check + (se suportado) vibracao leve via navigator.vibrate; ao finalizar o treino inteiro, uma animacao de conclusao satisfatoria (ex: check grande com fade + mensagem de parabens curta) e o registro do volume. 2) BARRA DE PROGRESSO do treino: durante a execucao, uma barra que preenche conforme as series sao concluidas, mostrando quanto falta. 3) Feedback de sucesso padronizado: quando uma acao importante e concluida (treino publicado, plano enviado, contrato assinado, pagamento registrado), mostrar um toast/confirmacao breve e elegante em vez de mudanca seca. 4) Tudo deve respeitar prefers-reduced-motion (reduzir/desligar animacoes e vibracao para quem prefere). Mantenha sutil e funcional, nunca poluindo a interface. Depois git add . && git commit -m "adiciona microinteracoes de progresso e feedback na execucao de treino e acoes chave" && git push origin main.
     Resultado: - `finishWorkout` chama `showWorkoutCompleteOverlay(volume)`: overlay escuro com blur, card que entra com spring animation (`cubic-bezier(0.34, 1.48, 0.64, 1)`), ├¡cone check animado, mensagem de parab├®ns, volume load d...

[ ] RELATORIO FINAL COMPLETO. Gere RELATORIO-FINAL.md consolidando tudo desta rodada (padronizacao + inovacoes): o que foi alterado em cada tarefa, o que ficou pendente ou nao foi possivel aplicar e por que, instrucoes de configuracao necessarias (ex: chaves VAPID das notificacoes push), e uma lista priorizada de proximos passos sugeridos para o orquestrador. Depois git add . && git commit -m "relatorio final consolidado da rodada de padronizacao e inovacoes" && git push origin main.
