==============================================
 FILA - TOKENIZACAO DE TEMA (SEGURA p/ rodar sozinho)
 Por que e segura sem supervisao: o criterio de sucesso e "NADA muda
 visualmente" - o app fica PIXEL-IDENTICO no tema atual. Nao ha escolha
 estetica para errar. Prepara o terreno para a lapidacao aba por aba e
 para os temas do white-label (trocar tema = trocar 4 variaveis).
 NAO e lapidacao (isso depende de aprovacao visual e fica para quando
 voce voltar). Esta fila so move cores fixas para variaveis.
 Cada tarefa = UMA linha. SEM seta no texto. ~7 numa janela.
 "session limit" ou erro de CLI = NAO rodou.
 Paleta atual (vira o tema padrao "gold"): brand #F59E0B,
 brand-strong #E7A83E, brand-soft rgba(245,181,27,0.1),
 brand-ink #1A1206 (texto sobre a marca), fundo #0D0D0D, card #1A1A1A,
 borda rgba(255,255,255,0.06), txt2 #9CA3AF, ok #10B981, erro #EF4444.
==============================================

== BACKUP ==
[OK] git checkout -b backup/tokenizacao-tema && git push origin backup/tokenizacao-tema && git checkout main. NAO altere arquivos. Confirme que o app abre sem erro de sintaxe. (sem commit adicional)
     Resultado: **Verifica├º├úo de sintaxe** | - `node --check` em todos os arquivos JS do projeto (`server.js`, `server/*.js`, `src/**/*.js`) ÔÇö sem erros de sintaxe. | - `index.html` (entry do PWA) presente na raiz. | O app abre s...

== TOKENIZACAO ==
[OK] DEFINIR variaveis canonicas de marca no :root (base.css e :root local do acesso.html): --brand #F59E0B, --brand-strong #E7A83E, --brand-soft rgba(245,181,27,0.1), --brand-ink #1A1206 (texto/icone sobre a marca). Se ja existem --dourado/--gold/--gold-soft/--accent, consolidar nessas 4 canonicas e fazer as antigas virarem alias apontando para elas. NAO mudar nada visual: resultado pixel-identico. git add . && git commit -m "feat(tema): variaveis canonicas de marca no root" && git push origin main
     Resultado: --gold-soft: var(--brand-soft); | ``` | One note worth flagging on `--brand-soft`: your spec wrote `rgba(245,181,27,0.1)`, but neither file uses that exact value ÔÇö base uses the brand hex at 0.1 (`rgba(245,158,11,0.1...
[OK] VARRER todo o CSS (styles.css, src/styles/*.css, acesso.html) trocando TODA cor de marca hardcoded (#F59E0B, #E7A83E e variantes do dourado em background, color, border, box-shadow, fill, stroke, gradiente) por var(--brand)/var(--brand-strong)/var(--brand-soft); texto/icone escuro sobre dourado vira var(--brand-ink). Listar ocorrencias por arquivo. Resultado pixel-identico. git add . && git commit -m "refactor(tema): cores de marca do css viram variaveis" && git push origin main
     Resultado: 1. **Leave as-is (recommended)** ÔÇö the brand is already tokenized; the rest are deliberately non-brand. No commit. | 2. **Tokenize only the decorative "elite gold" accents** (avatar/badge/progress: `#ffc84b`, `#ffd567...
[OK] VARRER app.js e demais JS por cores de marca em estilos inline, strings SVG, templates e graficos/sparklines (fill, stroke, background dourado): trocar por var(--brand...) quando CSS, ou por uma constante unica lida do tema quando valor JS, para graficos e SVG seguirem o tema. Resultado visual atual inalterado. git add . && git commit -m "refactor(tema): cores de marca no js e svg via variavel" && git push origin main
     Resultado: **Deixados de prop├│sito (mud├í-los alteraria o visual):** | - Paleta de avatares (`avatarColor`) e cores de categoria de atividade (treino/avalia├º├úo/atualiza├º├úo/retorno) ÔåÆ cores categ├│ricas n├úo-marca | - Acent...
[OK] CRIAR estrutura de temas por data-theme no elemento raiz: tema padrao "gold" (valores atuais) ativo, e deixar prontos dois exemplos inativos "ocean" (azul+rosa) e "graphite" - cada tema sobrescreve so --brand, --brand-strong, --brand-soft, --brand-ink. Trocar data-theme re-tematiza o app inteiro. Fixar em gold por enquanto (selecao por professor vem na frente de white-label). git add . && git commit -m "feat(tema): troca por data-theme com gold padrao e exemplos" && git push origin main
     Resultado: Blocos [data-theme="gold|ocean|graphite"] em src/styles/base.css (linhas 2021-2051), cada um sobrescrevendo so as canonicas de marca --brand/-strong/-soft/-ink (+ --brand-rgb, companion numerico que --brand-soft consome). gold=valores atuais (#F59E0B/#E7A83E/ink #1A1206), ocean=azul #0EA5E9 + rosa #F9A8D4, graphite=ardosia #64748B. <html data-theme="gold"> fixo em index.html e acesso.html. base.css carrega por ultimo: [data-theme] vence o :root de styles.css por ordem, re-tematizando o app inteiro. gold pixel-identico (mesmo valor que o :root ja resolvia). Estrutura ja entregue no commit 023609e; este so marca a tarefa. (RELATORIO-SEGURANCA-POS.md ficou fora: diff e so ruido de CRLF.)
[ ] GARANTIR contraste do texto sobre a marca via uma regra unica: todo elemento com fundo var(--brand) usa cor var(--brand-ink); revisar que nenhum lugar usa texto branco sobre dourado (bug recorrente); --brand-ink e a unica fonte dessa decisao por tema. git add . && git commit -m "fix(tema): contraste do texto sobre a marca via brand-ink" && git push origin main

== VERIFICACAO ==
[ ] VERIFICAR tematizacao completa: app pixel-identico com gold; como teste trocar data-theme para ocean e confirmar que TUDO muda de cor (sem sobrar dourado hardcoded em botoes, badges, icones, graficos, bolinhas), depois voltar para gold; listar qualquer dourado teimoso que nao re-tematizou e corrigir. git add . && git commit -m "test(tema): verificacao de tematizacao sem residuo" && git push origin main
