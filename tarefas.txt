==============================================
 LOTE - CONTINUACAO DO ALUNO (o que faltou + reverificar)
 Motivo: a sessao estourou na Leva 4 (reset 1pm). Rodaram de verdade:
 Levas 0,1,2,3 e parte da 4 (exercicio removido[?], lista de treinos,
 inputs carga/reps, execucao com contador). NAO rodaram: video do
 exercicio, conclusao+historico, Leva 5 inteira, Leva 6.
 Mais 3 SUSPEITAS que rodaram mas o Claude Code parou e perguntou em vez
 de fazer (reverificar).
 Cada tarefa = UMA linha. NAO quebrar ao colar. SEM seta no texto.
 "session limit"/erro de CLI = NAO rodou -> [OK] volta para [ ]. ~7 por janela.
 Tokens: dourado #F59E0B, fundo #0D0D0D, card #1A1A1A flat sem glow,
 borda rgba(255,255,255,0.06), radius 16px, txt2 #9CA3AF, ok #10B981,
 erro #EF4444.
==============================================

== BACKUP ==
[OK] git checkout -b backup/aluno-continuacao && git push origin backup/aluno-continuacao && git checkout main. NAO altere arquivos. Confirme que o app abre sem erro de sintaxe. (sem commit adicional)
     Resultado: - Branch `backup/aluno-continuacao` criado e publicado no GitHub. | - Retornado para `main`. | - `node --check app.js` saiu com c├│digo 0 ÔÇö **sem erros de sintaxe**. | O erro de `localStorage` mostrado antes ├® espe...

== REVERIFICAR (rodaram mas o Claude Code perguntou em vez de fazer) ==
[OK] REVERIFICAR o BUG "Exercicio removido" (na rodada anterior o Claude Code respondeu pedindo descricao em vez de corrigir, entao provavelmente NAO foi feito): no treino do aluno os exercicios podem aparecer como "Exercicio removido" quando getExercise(row.exerciseId) retorna undefined (renderWorkoutExercisePreview app.js:4752, renderPatternPreview 4726). Corrigir para exibir o NOME REAL do exercicio (grupo + meta series x reps x carga x descanso). Se o exercicio foi deletado da biblioteca, usar como fallback o nome salvo no proprio treino (snapshot) em vez de "Exercicio removido". Garantir IDs estaveis entre biblioteca e treino. git add . && git commit -m "fix(aluno): resolver exercicios reais no treino (fim do removido)" && git push origin main
[ ] REVERIFICAR botoes de acao dos cards (Leva 0 marcou "OK sem alteracao" apos a mensagem ter sido cortada - confirmar se realmente estao unificados): em Alunos os botoes de card eram outline finos e em Atualizacoes um retangulo dourado largo. Padronizar os botoes de acao DENTRO dos cards de todas as listas (Alunos, Atualizacoes, Dieta, Contratos, Padroes) para o mesmo estilo: outline dourado para acao secundaria, solido chapado para a acao principal, mesma altura e radius. Se ja estiverem iguais, confirmar e listar; se nao, corrigir. git add . && git commit -m "fix(unificacao): confirmar botoes de acao dos cards iguais entre abas" && git push origin main

== NAO RODARAM - FIM DA LEVA 4 (execucao) ==
[ ] VIDEO do exercicio na execucao: quando o exercicio tem video, o aluno assiste dentro do treino (player 16:9, play/pause) sem bugar; sem video, placeholder discreto (nao erro). Obs: o upload de video ainda nao persiste no servidor (backend, ver fim) - ate la, tratar ausencia com placeholder. git add . && git commit -m "feat(aluno): player de video do exercicio na execucao" && git push origin main
[ ] CONCLUSAO + HISTORICO: ao concluir o treino, registrar (data, volume, series concluidas) e refletir no Inicio ("ultimo treino concluido"), no Progresso e no perfil do aluno visto pelo gestor. Overlay de parabens premium ao finalizar. Este historico alimenta os widgets depois. git add . && git commit -m "feat(aluno): conclusao registra historico e evolucao" && git push origin main

  >>> VALIDAR COM VIDEO (caprichado): abrir treino, exercicios reais,
      inputs legiveis, executar serie, contador de descanso, concluir. <<<

== NAO RODARAM - LEVA 5 (mensagens, progresso, perfil) ==
[ ] MENSAGENS vira tela/aba propria (hoje so existe como sheet modal via openThreadSheet app.js:8129, sem fixMojibake/scrub). Criar acesso direto (icone de chat do header e/ou item de navegacao) que abre o thread no padrao do gestor: header com nome/avatar do personal + status, baloes (aluno a direita dourado, personal a esquerda escuro, horario + duplo check), compose bar fixa na base ACIMA da barra de navegacao. Aplicar fixMojibake e scrubVisibleText no conteudo. git add . && git commit -m "feat(aluno): mensagens como tela propria no padrao" && git push origin main
[ ] Mensagens - envio e push: enviar adiciona o balao na hora (otimista), rola pro fim, dispara push pro gestor; resposta do gestor em tempo real. Estado vazio "Inicie uma conversa com seu personal". git add . && git commit -m "feat(aluno): envio otimista de mensagem + push ao gestor" && git push origin main
[ ] PROGRESSO (funde Evolucao + Atualizacoes, renderStudentProgress app.js:5810 e renderStudentUpdates 5253): uma tela so com envio de atualizacao (peso + fotos frente/lado/costas + observacoes) que dispara push ao gestor; historico de atualizacoes; e evolucao (volume e peso corporal) com graficos no padrao. Remover a duplicidade apontada na auditoria. git add . && git commit -m "feat(aluno): tela de Progresso unindo evolucao e atualizacoes" && git push origin main
[ ] MAIS (hub) reorganizado (renderStudentProfile): lista organizada do que nao esta na barra - Perfil, Agenda, Contrato, Configuracoes - sem itens duplicados, com o botao Sair AQUI (movido do header). git add . && git commit -m "feat(aluno): hub Mais organizado sem duplicidade, com Sair" && git push origin main
[ ] PERFIL do proprio aluno: dados pessoais, objetivo, status do contrato e metricas (treinos na semana, volume, proximas atividades) em cards compactos no padrao do gestor; visualizar contrato (PDF) e dados da conta. Sem glow, encoding correto. git add . && git commit -m "feat(aluno): perfil do proprio aluno no padrao" && git push origin main

  >>> VALIDAR COM VIDEO: chat como aba com compose, progresso com envio,
      Mais sem duplicidade, perfil no padrao. <<<

== NAO RODOU - LEVA 6 (fechamento) ==
[ ] REVISAO FINAL premium do aluno: percorrer todas as telas (Login, Contrato, Inicio, Treinos, Execucao, Dieta, Mensagens, Progresso, Mais, Perfil, Agenda) garantindo paridade com o gestor: header com badge ALUNO e hamburguer; nenhum texto cortado; nenhuma caixa preta; nenhum botao escondido atras da barra; nenhum card fora do padrao; encoding correto; sem glow; barra estavel; contador da execucao funcionando; chat com compose. Atualizar AUDITORIA-ALUNO.md. git add . && git commit -m "chore(aluno): revisao final de consistencia premium" && git push origin main
