==============================================
 FILA DE TAREFAS - Personal Pro PWA
 RODADA: Acabamento Premium e Responsividade
 Formato: [ ] descricao da tarefa
 Nao altere linhas com [OK] ou [ERRO]
==============================================

[OK] Crie um ponto de retorno seguro antes das mudancas desta noite. Execute: git checkout -b backup-polish && git push origin backup-polish && git checkout main. Depois confirme com git branch -a que os branches existem. Verifique tambem que o app abre sem erros de sintaxe antes de prosseguir.
     Resultado: main                      (local + origin) ÔåÉ branch ativo
 | ```
 | **Sintaxe:** todos os 34 arquivos `.js` de `src/` + `sw.js` passaram sem erros.
 | Pode prosseguir com as mudan├ºas desta noite com seguran├ºa.


[OK] AREAS SEGURAS (notch e home indicator). No index.html garanta que a meta viewport inclui viewport-fit=cover, ficando exatamente: <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">. No styles.css aplique areas seguras em todos os elementos fixos: o header deve ter padding-top: env(safe-area-inset-top, 12px) para nunca invadir a barra de status do celular (hora, bateria, sinal); a barra de navegacao inferior deve ter padding-bottom: env(safe-area-inset-bottom, 8px) para nao ficar cortada pela barra de gestos do iPhone; o conteudo principal scrollavel deve ter padding-bottom de aproximadamente 90px mais safe-area-inset-bottom para que o ultimo item de qualquer lista nunca fique escondido atras da navegacao inferior; modais e bottom sheets devem respeitar safe-area-inset-bottom no rodape. Depois faca git add . && git commit -m "areas seguras para notch e home indicator" && git push origin main.
     Resultado: ```html
 | <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
 | ```
 | Isso j├í cobre as safe areas para notch e home indicator. N├úo h├í nada a alterar.


[OK] MENU LATERAL (corrigir bug critico de abrir e rolar). O menu lateral hoje e quase impossivel de abrir e quando aberto nao rola. Corrija no styles.css o seletor do drawer/sidebar com: position fixed, top 0, left 0, height 100dvh, width min(82vw, 300px), overflow-y auto, -webkit-overflow-scrolling touch, overscroll-behavior contain, z-index 9999, transform translateX(-100%) quando fechado e transform translateX(0) na classe .open, transition transform 0.28s ease, background #111111, padding-top env(safe-area-inset-top, 16px) e padding-bottom env(safe-area-inset-bottom, 16px). Garanta um overlay clicavel que fecha o menu ao tocar fora: .sidebar-overlay com position fixed, inset 0, background rgba(0,0,0,0.6), z-index 9998, opacity 0 e pointer-events none por padrao, e opacity 1 com pointer-events auto na classe .visible, com transition opacity 0.28s. No app.js verifique se o botao hamburguer realmente alterna a classe .open no sidebar e .visible no overlay, e se existe listener para fechar ao clicar no overlay. A area de toque do botao hamburguer deve ter no minimo 44x44px. Depois faca git add . && git commit -m "corrige menu lateral: abertura, scroll e overlay" && git push origin main.
     Resultado: | transition | `180ms ease` | `0.28s ease` |
 | | background | gradiente radial | `#111111` |
 | | padding safe-area | `max(1rem, env(...))` | `env(safe-area-inset-top, 16px)` / `env(safe-area-inset-bottom, 16px)` |
 | |...

[OK] RESPONSIVIDADE (telas pequenas a grandes). Nenhum elemento pode causar scroll horizontal: adicione overflow-x hidden no body e garanta box-sizing border-box em cards e linhas. Textos longos como nome de aluno, titulo de exercicio e preview de mensagem devem truncar com reticencias quando nao couberem usando white-space nowrap, overflow hidden e text-overflow ellipsis, aplicado especialmente nos nomes dentro dos cards e nos blocos de evento da agenda. Use padding lateral fluido nas telas com clamp(12px, 4vw, 20px) para adaptar de telas pequenas como iPhone SE a grandes como Pro Max. Titulos H1 com font-size clamp(28px, 8vw, 40px) para nao estourar em telas estreitas. Garanta que nada quebra a 320px de largura, que e o menor caso. Depois faca git add . && git commit -m "responsividade fluida e truncamento de textos" && git push origin main.
     Resultado: - **`.workspace-layout`**: `padding: 1rem clamp(12px, 4vw, 20px)` ÔÇö padding lateral fluido de 12px (iPhone SE) a 20px (Pro Max).
 | - **`.workspace-header`** (ambas as defini├º├Áes, tema base e dark): `clamp(12px, 4vw,...

[OK] ESTADOS VAZIOS PADRONIZADOS. O app mostra mensagens de lista vazia inconsistentes como "Nenhum plano alimentar criado", "Nenhuma conversa ainda" e "Sem historico". Crie uma classe reutilizavel .empty-state em styles.css com display flex em coluna, align-items center, text-align center, padding 32px 20px e gap 8px, contendo: icone grande de 48px com opacity 0.5 no topo, titulo curto em branco, uma linha de subtexto explicativa em cinza (#9CA3AF) e, quando fizer sentido, um botao de acao dourado para criar o primeiro item. Aplique esse mesmo componente em todas as telas com lista que pode estar vazia: Dieta, Mensagens, Contratos, Padroes, Atualizacoes, Historico e qualquer outra. Depois faca git add . && git commit -m "padroniza estados vazios em todas as telas" && git push origin main.
     Resultado: - Todas as telas com lista vazia passam agora o ├¡cone correspondente: `icons.messages`, `icons.contracts`, `icons.diet`, `icons.workouts`, `icons.library`, `icons.agenda`, `icons.updates`, `icons.progress`, `icons.stude...
     Resultado: | Evolu├º├úo por exerc├¡cio (l.4813) | `"Sem dados de carga"` | `"Nenhum dado de carga"` |
 | | Peso corporal (l.4817) | `"Sem peso registrado"` | `"Nenhum peso registrado"` |
 | | `renderConversation` (l.4945) | `"Sem m...

[OK] Continue d eonde parou: TRANSICOES E FEEDBACK DE TOQUE. Na troca de telas e abas adicione um fade sutil combinando opacity e leve translateY de cerca de 200ms ao renderizar uma nova view, para a troca nao ser seca. Modais e bottom sheets devem ter animacao de entrada e saida suaves (slide de baixo para cima em sheets, fade mais scale em modais). Todo elemento clicavel (cards, botoes, itens de lista) deve ter estado :active com leve reducao de scale usando transform scale(0.98) para dar sensacao tatil. Adicione -webkit-tap-highlight-color transparent para remover o flash cinza padrao do mobile. Respeite prefers-reduced-motion reduzindo ou desligando animacoes para quem tem essa preferencia ativada. Depois faca git add . && git commit -m "transicoes suaves e feedback de toque" && git push origin main.
     Resultado: **Backdrop** de modais e sheets anima opacity 0 ÔåÆ 1 na entrada, 1 ÔåÆ 0 na sa├¡da. | **:active t├ítil** ÔÇö todos os bot├Áes, cards e itens de lista t├¬m `transition: transform 100ms ease` + `:active { transform: scal...
     Resultado: - `showView()` ÔÇö adiciona `.is-entering` na view que chega, com reflow para reiniciar a anima├º├úo
 | - `renderManager()` / `renderStudent()` ÔÇö idem no `.content-area` ap├│s cada atualiza├º├úo de innerHTML
 | - `open...
     Resultado: You've hit your session limit ┬À resets 6:30am (America/Sao_Paulo)


[OK] ALVOS DE TOQUE E LEGIBILIDADE. Garanta que todos os botoes e icones clicaveis tenham area minima de 44x44px seguindo o padrao de acessibilidade mobile. O texto secundario nunca deve ficar abaixo de #9CA3AF sobre fundo escuro para manter contraste adequado. Itens de lista clicaveis devem ter no minimo 12px de padding vertical para evitar toques acidentais em itens vizinhos. Depois faca git add . && git commit -m "alvos de toque acessiveis e legibilidade" && git push origin main.
     Resultado: Commit `ab06e56` pushed. The conflicting `1.42rem !important` override was split so `.mini-button` now correctly enforces `2.75rem` while the non-interactive `.badge` keeps its compact size.
     


[ ] RELATORIO FINAL. Gere um arquivo RELATORIO-POLISH.md na raiz listando: o que foi alterado em cada uma das partes anteriores, qualquer ajuste que voce nao conseguiu aplicar e o motivo, e uma lista de pontos que ainda podem divergir das telas de referencia de design para revisao manual. Depois faca git add . && git commit -m "relatorio da rodada de acabamento" && git push origin main.
   

