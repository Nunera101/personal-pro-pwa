==============================================
 FILA - TREINO DO ALUNO (frente estrutural, RODAR PRESENTE)
 O que esta frente entrega: hoje o profissional so APLICA padroes; passa
 a poder CRIAR treino personalizado para o aluno (do zero) e a aplicar
 padrao como ponto de partida. Padrao = molde sem dono. Treino do aluno
 = pertence ao aluno. Aplicar padrao GERA COPIA INDEPENDENTE, SEM VINCULO
 (apagar o padrao depois NAO apaga o treino ja aplicado). A agenda, no
 tipo Treino, passa a listar os TREINOS DO ALUNO (nao padroes), e e
 OPCIONAL (pode agendar sem definir o treino).
 PROTOTIPOS APROVADOS pelo Guilherme (seguir fielmente): aba Treinos no
 perfil do aluno (ativos/arquivados + Criar treino); duas portas (Do zero
 / A partir de um padrao) com aviso de "sem vinculo"; Montador (nome,
 objetivo, nivel, exercicios com series/reps/carga/descanso, reordenar,
 rascunho vs publicar); Seletor de exercicios da biblioteca (busca,
 chips de grupo, selecao multipla, indica com/sem video).
 DIRETRIZ VISUAL: estilo "aplicativo" flat - sem degrade, sem glow,
 cards #1A1A1A, cantos arredondados, USANDO as variaveis de tema
 (var(--brand) etc. - NAO cor dourada hardcoded), para nao virar uma
 ilha visual. Texto sobre dourado = escuro.
 BASE JA EXISTENTE: a copia independente ao aplicar padrao (R-06) JA foi
 implementada na fila de seguranca - reutilizar, nao recriar.
 REGRAS CRITICAS:
 - RODAR PRESENTE, uma leva por janela. Toca dados e a relacao
   padrao/treino/agenda - nao rodar noturno.
 - Apos CADA leva: criar um treino, aplicar um padrao, e conferir no
   lado do ALUNO que o treino aparece. Se algo quebrar, PARAR e reverter.
 - NAO quebrar o que ja existe: a aba Padroes continua funcionando; o
   aluno continua vendo e executando treinos como hoje.
 - Construir em camadas (modelo de dados -> criar do zero -> aplicar
   padrao -> agenda), cada uma testavel antes da proxima.
 - SEM seta no texto. Cada tarefa = UMA linha.
 - "session limit" ou erro de CLI = NAO rodou.
==============================================

== BACKUP ==
[OK] git checkout -b backup/treino-do-aluno && git push origin backup/treino-do-aluno && git checkout main. NAO altere arquivos. Confirme que o app abre, que o login funciona, que a aba Padroes abre e que o aluno consegue ver um treino. (sem commit adicional)
     Resultado: Os erros `401` no console s├úo apenas as cargas de cole├º├úo **antes do login** (boot sem token) ÔÇö esperados; ap├│s o login as leituras autenticadas funcionam. | ## Limpeza | Como os dados ficam em `data/*.json` (git...

== LEVA T1 - MODELO DE DADOS E SEPARACAO PADRAO x TREINO (fundacao) ==
[OK] CLAREZA DE MODELO sem quebrar nada: garantir no codigo a distincao entre PADRAO (molde, sem studentId, vive na colecao de padroes/templates) e TREINO DO ALUNO (com studentId, vive na colecao de treinos/workouts). Documentar num comentario no topo das funcoes envolvidas qual e qual. NAO mudar comportamento ainda - so garantir que as duas entidades estao bem separadas no estado e na normalizacao, e que um treino do aluno tem os campos: id, studentId, professionalId, nome, objetivo, nivel, status (draft/published/archived), exercicios[], createdAt, updatedAt, e (opcional) origemPadraoId apenas como REFERENCIA HISTORICA que nunca cria dependencia. git add . && git commit -m "refactor(treino): separar claramente padrao (molde) de treino do aluno" && git push origin main
     Resultado: - **`isWorkoutPattern` / `getWorkoutPatterns` / `getStudentWorkouts`**: coment├írio deixando expl├¡cito qual ├® qual. | - **`buildStudentWorkoutFromPattern`**: refor├ºo de que `sourcePatternId` (origemPadraoId) ├® s├│ *...
[OK] CONFIRMAR a copia independente ao aplicar padrao (R-06 ja fez isso na seguranca): revisar que aplicar um padrao a um aluno gera um NOVO treino com novo id e studentId do aluno, copiando os exercicios por valor (deep copy), e que NAO ha referencia viva ao padrao; testar: aplicar padrao a um aluno, depois EDITAR o padrao e confirmar que o treino do aluno nao mudou; APAGAR o padrao e confirmar que o treino do aluno continua existindo e executavel. Se algo ainda vincular, corrigir. git add . && git commit -m "test(treino): aplicar padrao gera copia independente sem vinculo" && git push origin main
     Resultado: 2. `sourcePatternId/Title` s├úo strings (refer├¬ncia hist├│rica), e os arrays/objetos de exerc├¡cio n├úo s├úo os mesmos do padr├úo. | 3. **Editar** o padr├úo depois (t├¡tulo, carga, s├®ries, novo exerc├¡cio) n├úo altera...

  >>> TESTAR: aplicar padrao, editar/apagar o padrao, treino do aluno
      intacto. App e aluno normais. <<<

== LEVA T2 - ABA TREINOS NO PERFIL DO ALUNO + CRIAR DO ZERO ==
[OK] ABA TREINOS no perfil do aluno (visao do gestor): adicionar/identificar a aba "Treinos" dentro do perfil do aluno que lista os treinos DAQUELE aluno separados em ATIVOS e ARQUIVADOS, cada card com inicial/letra do treino, nome, numero de exercicios, duracao estimada, badge de status (Publicado/Rascunho) e ultima execucao, e botoes Editar / Ver / menu (...). Botao "Criar treino" no topo. Seguir o prototipo aprovado: estilo flat app, cards #1A1A1A, cores via variaveis de tema. Estado vazio elegante quando o aluno nao tem treinos. git add . && git commit -m "feat(treino): aba treinos no perfil do aluno com ativos e arquivados" && git push origin main
     Resultado: - `src/styles/treinos.css` ÔÇö estilos `.pw-*` (grupos, cards, thumb, a├º├Áes, menu) | - `index.html` ÔÇö bump de cache `app.js?v=62` | Validei a sintaxe com `node --check app.js`. N├úo rodei a UI; se quiser, posso usa...
[OK] DUAS PORTAS ao criar treino: o botao "Criar treino" abre um sheet com duas opcoes - "Criar do zero" (monta personalizado) e "A partir de um padrao" (usa molde como ponto de partida) - com um aviso claro de que ao usar um padrao ele vira um treino proprio do aluno e que editar/apagar o padrao depois NAO afeta este treino. Seguir o prototipo aprovado. git add . && git commit -m "feat(treino): duas portas de criacao com aviso de independencia" && git push origin main
     Resultado: - Os dois bot├Áes est├úo abrindo o **mesmo modal** quando deveriam ser diferentes (ou vice-versa)? | - Um dos bot├Áes "Criar" est├í com **bug** (n├úo abre, salva no lugar errado)? | - Voc├¬ quer **unificar** as duas po...
[OK] MONTADOR DE TREINO (criar do zero / editar): reaproveitar o montador existente (o mesmo usado para padrao), mas salvando como TREINO DO ALUNO (com studentId). Campos: nome, objetivo, nivel, e lista de exercicios, cada um com series, reps, carga e descanso, com opcao de remover e de REORDENAR por arrastar; rodape com "Salvar rascunho" (status draft, invisivel ao aluno) e "Publicar treino" (status published, visivel ao aluno); duracao estimada calculada. Seguir o prototipo aprovado (estilo flat, variaveis de tema). git add . && git commit -m "feat(treino): montador salva treino personalizado do aluno" && git push origin main
     Resultado: - **`src/styles/treinos.css`**: estilos da al├ºa (cursor grab, `touch-action: none`) e do estado `is-dragging`. | - **`index.html` / `sw.js`**: alinhei os cache-busters que estavam inconsistentes (index em v62/v55, SW e...
[OK] SELETOR DE EXERCICIOS da biblioteca (dentro do montador): sheet com busca, chips de grupo muscular para filtrar, lista com SELECAO MULTIPLA (varios de uma vez), cada item indicando se tem video ou nao, barra fixa no rodape com contador de selecionados e botao "Adicionar ao treino", e atalho "Criar novo exercicio na biblioteca". Os exercicios adicionados entram no montador com valores padrao de series/reps que o profissional ajusta. Seguir o prototipo aprovado. git add . && git commit -m "feat(treino): seletor multiplo de exercicios da biblioteca" && git push origin main
     Resultado: - Mantive o `<select>` por linha para **trocar** o exerc├¡cio de uma linha j├í existente; o picker ├® **aditivo**. | - Se a biblioteca ativa estiver vazia, o picker n├úo abre e mostra um toast orientando a cadastrar exe...

  >>> TESTAR: criar um treino do zero para o aluno, adicionar exercicios,
      publicar, e CONFIRMAR no lado do aluno que o treino aparece e e
      executavel. Salvar rascunho NAO aparece para o aluno. <<<

== LEVA T3 - APLICAR PADRAO PELA NOVA PORTA + AGENDA ==
[OK] APLICAR PADRAO pela porta "A partir de um padrao": ao escolher essa opcao, mostrar a lista de padroes disponiveis (cards no mesmo estilo), e ao escolher um, ABRIR O MONTADOR ja preenchido com os exercicios copiados do padrao (copia por valor), permitindo o profissional ajustar antes de publicar para o aluno; ao publicar, vira treino do aluno independente (sem vinculo, conforme T1). Deixar claro na tela que e uma copia editavel. git add . && git commit -m "feat(treino): aplicar padrao abre montador pre-preenchido editavel" && git push origin main
     Resultado: - 4 testes de `aplicar-padrao-copia` (independ├¬ncia R-06): **passam**. | - Commit + push feitos (`e2fe90a`). | ÔÜá´©Å **N├úo rodei a UI no navegador** ÔÇö conforme a regra da leva ("criar um treino, aplicar um padr├úo...
[OK] AGENDA lista TREINOS DO ALUNO (nao padroes): no Agendar atividade, quando o tipo for Treino, o campo "qual treino" passa a listar os TREINOS PUBLICADOS DAQUELE ALUNO (nao os padroes), e e OPCIONAL - o profissional pode agendar um treino sem especificar qual, decidindo na hora. Se o aluno nao tiver treinos ainda, mostrar atalho para criar. Seguir o prototipo aprovado do Agendar (grade de tipos com cores, campo de treino condicional ao tipo Treino). git add . && git commit -m "feat(agenda): tipo treino lista treinos do aluno, opcional" && git push origin main
     Resultado: ## Observa├º├Áes | - **N├úo rodei a UI no navegador** ÔÇö validei apenas a sintaxe, conforme o padr├úo das levas anteriores. | - Deixei de fora do commit a altera├º├úo pendente em `RELATORIO-SEGURANCA-POS.md` (uma inde...
[OK] COMUNICACAO entre as abas sem bug: garantir o fluxo redondo - criar/publicar treino reflete imediatamente na lista de treinos do aluno, no que o aluno ve, e na lista de treinos disponivel ao agendar; arquivar um treino tira ele dos publicados (some pro aluno e da agenda) mas mantem no historico; o badge de "treinos" e contadores do perfil do aluno atualizam. Testar a cadeia inteira sem recarregar manualmente. git add . && git commit -m "fix(treino): sincronizacao entre treinos, aluno e agenda" && git push origin main
     Resultado: - `renderAgendaCompact` | Agora **arquivar some pro aluno e da agenda**, mas o agendamento continua vis├¡vel como hist├│rico ÔÇö exatamente o "fluxo redondo" pedido. | `node --check` passou; cache bump `app.js`/`sw` Ôå...

  >>> TESTAR (cadeia completa): criar do zero -> publica -> aluno ve;
      aplicar padrao -> ajusta -> publica -> aluno ve; agendar treino
      listando os treinos do aluno; arquivar some pro aluno mas fica no
      historico; editar/apagar padrao nao afeta treinos aplicados. <<<

== FECHAMENTO ==
[OK] Revisao da frente: conferir que padrao e treino do aluno sao independentes, que o profissional cria do zero e por padrao, que a agenda lista treinos do aluno (opcional), que nada do que existia quebrou (aba Padroes ok, aluno executa treino ok), e que o visual segue o estilo flat aprovado usando variaveis de tema. Atualizar AUDITORIA-ALUNO.md e DIVERGENCIAS-GESTOR.md com a nova capacidade de criar treino do aluno. git add . && git commit -m "chore(treino): revisao da frente de treino do aluno" && git push origin main
     Resultado: Frente conferida no codigo. Independencia: isWorkoutPattern (sem studentId) x treino com studentId; buildStudentWorkoutFromPattern faz copia profunda (R-06), sourcePatternId so referencia historica. Criar do zero (porta "Criar treino" -> montador scope student) e por padrao (porta "Aplicar padrao" -> cards -> montador pre-preenchido editavel) ambos via openWorkoutForm. Agenda: workoutFieldContent lista publicados do aluno (nunca padroes), opcional "Decidir na hora". Nada quebrou: node -c app.js OK; renderManagerWorkouts lista so getWorkoutPatterns; eyebrow "Elite AS" oculto (display:none em base.css); isAgendaWorkoutStartable faz arquivados sumirem; execucao do aluno intacta. Visual: .pw-card/montador usam var(--card/--borda/--brand/--brand-ink), sem glow. AUDITORIA-ALUNO.md (secao 8) e DIVERGENCIAS-GESTOR.md (Padroes + rastreamento) atualizados. NAO rodei a UI no navegador (so revisao estatica + node -c).
