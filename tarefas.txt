==============================================
 FILA NOTURNA MASSIVA - PREPARO DAS 3 FRENTES (seguro p/ rodar sozinho)
 Regra de ouro desta fila: NADA que quebre o app atual. Tudo aqui ou
 SO LE e gera relatorio .md, ou ADICIONA camada sem ativar (default =
 comportamento de hoje), ou e visual isolado. NENHUMA tarefa altera
 login/permissoes de forma que possa travar o acesso. Se uma tarefa
 exigir decisao de arquitetura, o Claude deve DOCUMENTAR a recomendacao
 num .md e seguir, NUNCA adivinhar e refatorar a espinha.
 Cada tarefa = UMA linha. SEM seta no texto. "session limit"/erro de
 CLI = NAO rodou. Ordem: relatorios primeiro (risco zero), depois
 preparacao nao-destrutiva, depois visual/fotos, depois hardening.
 Tokens: dourado #F59E0B, fundo #0D0D0D, card #1A1A1A flat sem glow,
 borda rgba(255,255,255,0.06), radius 16px, txt2 #9CA3AF, ok #10B981,
 erro #EF4444. Texto sobre dourado SEMPRE escuro #1A1206.
==============================================

== BACKUP ==
[OK] git checkout -b backup/preparo-frentes && git push origin backup/preparo-frentes && git checkout main. NAO altere arquivos. Confirme que o app abre sem erro de sintaxe. (sem commit adicional)
     Resultado: Tudo certo: | - Branch `backup/preparo-frentes` criado e publicado no GitHub | - De volta em `main` (sem altera├º├Áes de arquivo, sem commit adicional) | - **Todos os 50 arquivos JS** (`server/` + `src/`) passaram na ...

== BLOCO 1 - RELATORIOS E AUDITORIAS (so leem, geram .md, risco zero) ==
[OK] Gerar RELATORIO-SEGURANCA.md auditando o estado de seguranca atual SEM alterar codigo: como funciona o login/auth hoje (onde valida, o que e cliente vs servidor), quais rotas do server/ exigem autenticacao e quais nao, onde dados sensiveis trafegam (CPF, contrato, financeiro), uso de HTTPS, exposicao de chaves/segredos no front, validacao de input nas rotas, e uma lista priorizada (critico/medio/baixo) de recomendacoes para o futuro login seguro e para multi-personal. So documentar. git add . && git commit -m "docs: relatorio de seguranca atual" && git push origin main
     Resultado: - Sem schema de valida├º├úo de body nas rotas | - Logout puramente client-side (sem blocklist de JWT) | - Audit log n├úo registra logins/falhas | - `TRAINER_ID = "trainer-demo"` hardcoded ÔÇö bloqueador para multi-per...
[OK] Gerar RELATORIO-MULTI-PERSONAL.md mapeando TUDO que precisa mudar para o app aceitar varios personais com dados isolados, SEM alterar codigo: quais colecoes/objetos hoje sao globais (alunos, treinos, dieta, contratos, financeiro, mensagens, biblioteca, padroes, agenda) e precisariam de um vinculo professionalId; onde no app.js e no server/ os dados sao lidos/escritos; quais consultas precisariam filtrar por professionalId; o que a area do admin precisaria ler; e um plano de migracao em fases (adicionar campo com default, popular, ativar filtro, validar). So documentar com arquivo:linha. git add . && git commit -m "docs: relatorio de prontidao para multi-personal" && git push origin main
     Resultado: - `app_collections` (tabela de fallback JSON) n├úo tem `trainer_id` ÔÇö precisa de coluna nova. | **O que precisa de filtro nas queries (Fase 2):**   | `GET/PUT/DELETE /collections/:collection` (`api.js:894ÔÇô935`), lo...
[OK] Gerar RELATORIO-ISOLAMENTO.md (auditoria de vazamento de dados como ja discutido) SEM alterar codigo: para cada area (treino, dieta, agenda, mensagens, progresso, perfil, contrato, financeiro, biblioteca, padroes), verificar se a leitura/escrita filtra pelo dono correto, se ha estado global que mistura usuarios, se aplicar treino/plano gera copia independente, e se um usuario consegue acessar dado de outro via id manipulado. Listar cada risco com arquivo:linha e recomendacao. git add . && git commit -m "docs: relatorio de isolamento de dados" && git push origin main
     Resultado: | Contrato | `mergeStudentContracts()` retorna contratos de outros alunos por falha de l├│gica em `api.js:206` | | | Financeiro | `openPaymentForm()` e `handlePaymentFormSheet()` exp├Áem pagamentos alheios | | | Geral ...
[OK] Gerar RELATORIO-ABAS.md percorrendo CADA aba primaria e suas telas secundarias (sheets/modais) dos dois perfis - Dashboard, Alunos (+Novo/Editar aluno, Enviar link), Agenda (+Agendar atividade, Detalhe do evento), Padroes (+Montador, Aplicar), Biblioteca (+Novo/Editar exercicio, player), Atualizacoes (+Avaliar check-in), Contratos (+Novo contrato, Visualizar), Financeiro (+Registrar pagamento, Relatorios), Dieta (+Plano alimentar), Mensagens, Configuracoes, e no aluno Inicio, Treinos (+Detalhe, Execucao), Dieta, Agenda, Progresso, Chat, Mais, Perfil, Contrato/Gate - documentando para cada uma: estado visual atual, inconsistencias, itens cortados/vazando, e o que falta para ficar premium. Este e o mapa que vai guiar a lapidacao aba por aba. So documentar. git add . && git commit -m "docs: relatorio do estado de todas as abas e secundarias" && git push origin main
     Resultado: `RELATORIO-ABAS.md` gerado e commitado. O documento cobre **21 se├º├Áes** ÔÇö todas as abas prim├írias e secund├írias dos dois perfis: | **Personal (Manager):** Dashboard, Alunos (+Novo/Editar, Enviar link), Agenda (+Ag...
[OK] Gerar RELATORIO-ADMIN.md desenhando a area do admin (dono do sistema) SEM implementar: o que o admin precisa ver e fazer - listar/criar/editar perfis de professores, ver total de alunos no sistema, financeiro consolidado (soma de todos) e por professor, acessar a visao de um professor especifico isoladamente, metricas gerais (professores ativos, alunos por professor, faturamento total); propor a estrutura de dados (perfil professor, vinculo admin), as telas necessarias e como reaproveitar os componentes existentes (cards de metrica, listas, relatorios) para o admin. So documentar. git add . && git commit -m "docs: desenho da area do admin" && git push origin main
     Resultado: - Nova tabela `saas_payments` para o financeiro do SaaS (separado do financeiro interno dos trainers) | **7 telas** ÔÇö Dashboard, Lista de Trainers, Perfil do Trainer, Impersona├º├úo, Financeiro Consolidado, Relat├│rio...

== BLOCO 2 - PREPARACAO NAO-DESTRUTIVA MULTI-PERSONAL (adiciona sem ativar) ==
[OK] Adicionar o conceito de professionalId de forma NAO-DESTRUTIVA, sem mudar comportamento: criar um id do profissional atual (o personal unico de hoje) e, na normalizacao dos objetos (alunos, treinos, dieta, contratos, financeiro, padroes, biblioteca, agenda, mensagens), gravar professionalId com DEFAULT apontando para esse profissional atual quando o campo nao existir. NAO criar filtro por professionalId ainda (o app continua mostrando tudo, identico a hoje) - so popular o campo para o futuro. Documentar no fim quais objetos passaram a ter o campo. git add . && git commit -m "feat(dados): campo professionalId com default nao-destrutivo" && git push origin main
     Resultado: Campo `professionalId: objeto.professionalId || TRAINER_ID` adicionado em 10 funcoes de normalizacao (normalizeStudent, normalizeExercise, normalizeWorkout, normalizeActivity, normalizeSession, normalizeUpdate, normalizeContract, normalizeMessage, normalizePayment, normalizeDietPlan). Sem filtro, sem quebra. Documentado em RELATORIO-PROFESSIONALID.md.
[ ] Criar a CAMADA de acesso a dados preparada para filtro por dono, ainda inativa: funcoes utilitarias (ex getDataScope) que hoje retornam TUDO (escopo global = comportamento atual) mas centralizam por onde os dados sao lidos, de modo que ativar o multi-personal no futuro seja trocar o escopo num lugar so. Refatorar as leituras para passar por essas funcoes SEM mudar o resultado atual. Documentar os pontos centralizados. git add . && git commit -m "refactor(dados): camada de escopo centralizada (global por enquanto)" && git push origin main

== BLOCO 3 - FOTOS / AVATAR (visual isolado) ==
[ ] FOTO DE PERFIL completa e isolada (estende o que ja foi iniciado): upload de foto no perfil (gestor e aluno) com preview, recorte/centralizacao simples, compressao no cliente para um tamanho razoavel (ex max 512px, qualidade media) antes de salvar, e persistencia; a foto aparece no avatar do header e nos lugares onde o usuario e representado (lista de alunos, conversa, perfil); fallback para as iniciais quando nao houver foto. Preparar o campo de foto para reuso futuro (aba Comunidade) sem criar a aba. Garantir que nao quebra o avatar de iniciais atual. git add . && git commit -m "feat(perfil): foto de perfil com compressao, preview e fallback" && git push origin main
[ ] AVATARES consistentes em todo o app: onde hoje aparece o circulo de iniciais (header, listas, conversas, cards), padronizar um componente unico de avatar que mostra a foto se houver ou as iniciais com cor estavel por usuario; mesmo tamanho/estilo coerente em cada contexto. git add . && git commit -m "feat(perfil): componente de avatar unico foto-ou-iniciais" && git push origin main

== BLOCO 4 - HARDENING DEFENSIVO (so adiciona protecao, nao muda fluxo) ==
[ ] Validacao e sanitizacao de input nas rotas do server/ SEM mudar o fluxo: garantir que toda rota valida tipos e tamanhos do que recebe (campos obrigatorios, limites de tamanho de texto, formato de email/CPF) e rejeita payload malformado com erro claro, em vez de confiar no cliente; nao alterar as regras de quem pode o que (isso e a frente de seguranca futura), so endurecer a entrada. Listar as rotas endurecidas. git add . && git commit -m "fix(seguranca): validacao e sanitizacao de input nas rotas" && git push origin main
[ ] Tratamento de erros e limites defensivos: garantir que falhas de rede/servidor no front mostram mensagem amigavel (nunca tela branca/preta nem erro cru), que uploads respeitam limite de tamanho com aviso claro, e que campos de texto longos tem limite no front coerente com o servidor; adicionar try/catch onde faltar nas operacoes de IO. NAO mexer em push/VAPID nem em login. git add . && git commit -m "fix(robustez): tratamento de erros e limites defensivos" && git push origin main
[ ] Gerar RELATORIO-FINAL-NOTURNO.md resumindo tudo que esta fila fez: relatorios gerados, campos/camadas adicionados (e que estao INATIVOS por padrao), fotos e avatar, hardening aplicado, e uma lista clara do que ficou PRONTO PARA o Guilherme decidir junto (ativar multi-personal, construir admin, login seguro) com os respectivos relatorios de referencia. git add . && git commit -m "docs: relatorio final da fila noturna de preparo" && git push origin main
