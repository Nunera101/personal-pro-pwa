== LEVA S2 - OWNERSHIP NO SERVIDOR (o coracao, formato multi-tenant) ==
[OK] CRIAR o utilitario central de ownership no server/ (estrutural, reutilizavel): uma funcao assertOwnership(resource, auth) e helpers filterOwned(list, auth) que decidem se um recurso pertence ao requisitante pela regra unica - manager: resource.trainerId === auth.trainerId; student: resource.trainerId === auth.trainerId E resource.studentId === auth.studentId. Centralizar num modulo (ex server/ownership.js) para que NENHUMA rota reimplemente a checagem. Cobrir o caso de recurso inexistente (404) e nao-autorizado (403). Escrever um teste unitario do utilitario (varios cenarios: dono, outro aluno, outro trainer, recurso inexistente). git add . && git commit -m "feat(seguranca): modulo central de ownership com testes" && git push origin main
     Resultado: - `manager`: `resource.trainerId === auth.trainerId` | - `student`: `resource.trainerId === auth.trainerId` **E** `resource.studentId === auth.studentId` | **`server/ownership.test.js`** ÔÇö 10 cen├írios cobrindo: dono...
[OK] APLICAR ownership no DELETE de colecao (R-19, o vetor mais destrutivo, api.js:912-935): antes de deletar qualquer item, carregar o recurso e passar por assertOwnership; aluno so deleta o proprio, manager so do proprio trainer; recurso de outro = 403, inexistente = 404. Escrever teste de ataque: simular aluno A tentando deletar item do aluno B e confirmar 403; manager do trainer X tentando deletar item do trainer Y e confirmar 403. git add . && git commit -m "fix(seguranca): ownership no delete de colecao com teste de ataque" && git push origin main
     Resultado: - Ô£à caminhos leg├¡timos (pr├│prio aluno / pr├│prio trainer) deletam s├│ o item certo | - Ô£à `EXERCISES_KEY` permanece fora do ownership | 16/16 testes passando. `api.js` carrega sem erro. Commit e push para `main` f...
[OK] APLICAR ownership no PUT de item individual e na escrita de colecao (R-02, R-05, R-17, api.js:902-910): toda escrita valida que o recurso alvo pertence ao requisitante antes de gravar; o studentId/trainerId do recurso vem do SERVIDOR (sessao), nunca e aceito do corpo do cliente para reatribuir dono - se o corpo tentar mudar o dono, ignorar e manter o do servidor. Teste de ataque: tentar reatribuir um treino/pagamento para outro studentId via corpo e confirmar que o servidor recusa/ignora. git add . && git commit -m "fix(seguranca): ownership na escrita, dono vem do servidor" && git push origin main
     Resultado: novo helper central enforceOwnerOnWrite em server/ownership.js (update preserva dono ja gravado, create carimba trainerId da sessao, id de outro dono descartado). Aplicado no caminho do manager em writeCollectionForAuth (workouts/payments/diets/activities/sessions/updates/contracts/messages) + mergeStudentsForWrite carimba trainerId. EXERCISES (biblioteca) fica fora. server/write-ownership.test.js: ataques de reatribuicao de treino/pagamento para outro studentId e trainerId sao ignorados. 23/23 testes passando.
[ ] CORRIGIR mergeStudentContracts que vaza contratos alheios (R-14, api.js:206): trocar a logica para filtrar primeiro pelos contratos do proprio aluno e so depois mesclar, de modo que contratos de outros alunos NUNCA estejam no resultado. Teste de ataque: aluno A pede merge e confirma que so recebe os proprios contratos, nenhum do aluno B. git add . && git commit -m "fix(seguranca): mergeStudentContracts sem vazamento (R-14)" && git push origin main
[ ] CORRIGIR login de aluno para multi-tenant correto (RELATORIO-MULTI-PERSONAL secao 6, api.js:536): buscar o aluno por e-mail e usar o trainerId DO PROPRIO REGISTRO encontrado para montar o JWT, em vez de qualquer constante; se houver e-mails iguais em trainers diferentes (colisao), tratar de forma segurae determinada (documentar a escolha). Isso ja deixa o login pronto para multiplos personais. Teste: login de aluno gera token com o trainerId correto do registro. git add . && git commit -m "fix(seguranca): login de aluno usa trainerId do registro" && git push origin main

  >>> PARAR E TESTAR: login gestor e aluno funcionam? Os testes de ataque
      passam (ataques recebem 403)? App normal? Se sim, seguir. <<<

== LEVA S3 - UPLOADS, VIDEOS E TOKENS (fechar portas publicas) ==
[ ] PROTEGER /uploads/ (C3): PDFs de contrato, fotos de perfil e progresso nao podem ser acessiveis por URL publica adivinhavel. Servir esses arquivos por uma rota autenticada que verifica ownership (o solicitante so acessa arquivo do proprio escopo) OU por URL assinada temporaria; manter compatibilidade para o que ja existe. Teste de ataque: tentar acessar um PDF de contrato de outro aluno por URL direta e confirmar bloqueio. git add . && git commit -m "fix(seguranca): uploads protegidos por auth e ownership" && git push origin main
[ ] PROTEGER GET /api/videos/:id (C4, videos.js:49): exigir autenticacao e verificar que o video pertence ao trainer do solicitante (ou que o aluno tem acesso ao exercicio referenciado); video de outro trainer = 403. Manter o suporte a Range request (streaming iOS) ja existente. Teste de ataque: usuario sem permissao tenta baixar video por id e recebe 403. git add . && git commit -m "fix(seguranca): video protegido por auth e ownership" && git push origin main
[ ] CONSUMIR tokens de uso unico (A1): marcar como usado (usedAt) o token de student-area-view apos o primeiro acesso bem-sucedido (hoje fica reutilizavel por 7 dias, api.js:782); reduzir o TTL do link de area do aluno para 24-48h (M6). Validar que o reset-password ja consome corretamente e alinhar o padrao. Teste: usar o link uma vez funciona, segunda vez e recusado. git add . && git commit -m "fix(seguranca): tokens de area do aluno de uso unico e TTL menor" && git push origin main
[ ] VALIDAR trainerId de uploads contra a sessao (M5, uploads.js:89): o trainerId associado a um upload vem SEMPRE de req.auth, nunca do corpo do cliente; se o corpo enviar um trainerId diferente, ignorar. Teste: tentar associar upload a outro trainer via corpo e confirmar que o servidor usa o da sessao. git add . && git commit -m "fix(seguranca): trainerId de upload vem da sessao" && git push origin main

  >>> PARAR E TESTAR: upload de foto/PDF/video do gestor e do aluno
      continuam funcionando normalmente? Ataques bloqueados? <<<

== LEVA S4 - HARDENING DE PLATAFORMA (blindagem geral) ==
[ ] ADICIONAR helmet e headers de seguranca (A3): instalar helmet no Express e configurar Content-Security-Policy compativel com o app (scripts/styles/img/connect que o app realmente usa, incluindo cdnjs do jsPDF e o proprio backend), HSTS, X-Frame-Options (anti-clickjacking), X-Content-Type-Options nosniff. Testar que o app carrega 100% (sem recurso bloqueado pela CSP) antes de commitar; se algo for bloqueado, ajustar a CSP, nunca desligar. git add . && git commit -m "feat(seguranca): helmet com CSP, HSTS e anti-clickjacking" && git push origin main
[ ] RESTRINGIR CORS (A4): trocar o cors aberto (origin *) por uma allowlist das origens reais do app (dominio de producao no Railway + localhost de dev), tanto no Express quanto no Socket.IO. Testar que o app em producao e em dev continua conectando. git add . && git commit -m "fix(seguranca): CORS restrito a origens conhecidas" && git push origin main
[ ] VALIDACAO DE SCHEMA no corpo das rotas criticas (M1): adicionar validacao de tipos/campos/limites (pode ser uma checagem manual central ou uma lib leve) em /auth/login, /auth/reset-password, e nas rotas de escrita de colecao; payload malformado retorna 400 com mensagem clara, sem tocar o banco. Reaproveitar os validadores de input ja criados no hardening anterior. git add . && git commit -m "feat(seguranca): validacao de schema nas rotas criticas" && git push origin main
[ ] ESCAPE de HTML no frontend contra XSS (M2): auditar o app.js do front e garantir que TODO conteudo livre de usuario (nome de aluno, observacoes, corpo de mensagem, titulo de treino/plano) seja renderizado via textContent ou uma funcao escHtml, nunca via innerHTML sem escape. Listar os pontos corrigidos. NAO alterar o visual. git add . && git commit -m "fix(seguranca): escape de html contra xss no front" && git push origin main
[ ] AUDIT LOG de autenticacao (M4): registrar no audit log tentativas de login (sucesso e falha), logout e uso de links de token, com horario e identificador; garantir que funciona mesmo no modo de armazenamento atual (nao falhar silenciosamente). git add . && git commit -m "feat(seguranca): audit log de login, falha e tokens" && git push origin main

  >>> PARAR E TESTAR: app carrega 100% com a CSP (nenhum recurso
      bloqueado)? Login, chat em tempo real e uploads funcionam? <<<

== LEVA S5 - GUARDAS NO FRONTEND (defesa em profundidade) ==
[ ] GUARDA DE DONO nos getters do app.js (R-01 a R-08, R-10, R-16): adicionar verificacao de propriedade em getWorkout (1390), getDietPlan (1908), deleteActivity (13020), openAgendarSheet (8735), markUpdateViewed (13256), openPaymentForm (10341) e derivados, retornando nulo/aviso quando o recurso nao pertence ao usuario atual. Isto e defesa em profundidade (o servidor ja protege na S2); melhora a UX e fecha o IDOR via console. NAO mudar o visual. git add . && git commit -m "fix(seguranca): guardas de dono nos getters do front" && git push origin main
[ ] BLOQUEAR escalada de role no front (R-11): handleUpdateComment e qualquer acao restrita ao gestor checam state.currentUser.role === manager antes de agir; aluno recebe aviso e nao executa. git add . && git commit -m "fix(seguranca): acoes de gestor bloqueadas para aluno no front" && git push origin main
[ ] CLONAR ao aplicar plano/treino padrao (R-06): ao aplicar um padrao (studentId vazio) a um aluno, gerar uma COPIA independente com novo id em vez de reatribuir o objeto-pai, para que editar o padrao depois nao afete retroativamente os alunos. Teste: aplicar padrao a dois alunos, editar o padrao, confirmar que os treinos dos alunos nao mudaram. git add . && git commit -m "fix(dados): aplicar padrao gera copia independente (R-06)" && git push origin main

== FECHAMENTO ==
[ ] Gerar RELATORIO-SEGURANCA-POS.md: para cada um dos 21 riscos do RELATORIO-ISOLAMENTO e cada item critico/alto do RELATORIO-SEGURANCA, marcar RESOLVIDO (com o commit) ou PENDENTE (com motivo); listar os testes de ataque criados e seu resultado; e listar o que fica para a fase de lancamento (senha admin por primeiro acesso, blocklist de JWT/refresh tokens, 2FA, eliminar hash legado). git add . && git commit -m "docs: relatorio de seguranca pos-correcoes" && git push origin main
