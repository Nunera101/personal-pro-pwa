== CONTRATO (nao rodaram) ==
[OK] CONFIRA SE DEU CERTO: NOVO CONTRATO GERA PDF AUTOMATICAMENTE: refazer o fluxo Novo contrato do gestor: ele seleciona o aluno, escolhe o MODELO do servico (cadastrado nas Configuracoes), e preenche apenas plano, valor e vigencia; o app entao substitui TODOS os placeholders do modelo pelos dados reais (aluno do cadastro, profissional das Configuracoes, valores digitados) e GERA o PDF do contrato no proprio app usando jsPDF via cdnjs (texto formatado, titulo, paragrafos com quebra correta, dados completos, rodape com versao e data); o PDF gerado e salvo pelo MESMO caminho do pdfUrl atual (mesmo service usado no upload manual), de modo que viewer, gate e aceite com CPF continuem funcionando sem mudanca; mostrar PRE VISUALIZACAO do PDF gerado para o gestor antes de enviar para assinatura; manter o upload manual de PDF como opcao alternativa secundaria; placeholder sem dado correspondente nao aparece no documento, sem chaves vazias. git add . && git commit -m "feat(contratos): geracao automatica de PDF a partir de modelo e dados" && git push origin main
     Resultado: O upload de contrato (inclusive o manual j├í existente) montava a URL como `${state.apiBase}/api/uploads/contracts`, mas `state.apiBase` j├í termina em `/api` ÔÇö resultava em `/api/api/...` e **404 em produ├º├úo**. Corr...
     Conferido em 12/06/2026 (Playwright, fluxo completo no app real): login gestor, Novo contrato, picker de aluno, modo "Gerar do modelo" pre-selecionado, geracao via jsPDF cdnjs OK, status "PDF gerado e anexado", preview com iframe, PDF real (%PDF) salvo em /uploads/contracts com Teste/Elite/R$350,00/12/12/2026/rodape v1.0 e "Gerado em", zero placeholders sobrando; guards OK (sem modelo, sem valor, dados mudados apos gerar bloqueia envio); troca para "Anexar PDF pronto" mostra dropzone e invalida o doc gerado; envio para assinatura criou contrato com pdfUrl em producao (removido apos o teste, era so verificacao). ATENCAO: em localhost o app usa a API DE PRODUCAO do Railway (getApiBases prioriza REMOTE_API_BASE) ÔÇö testes locais criam dados reais.
[ ] GATE DO ALUNO COM O PDF GERADO: confirmar que o aluno, no gate de primeiro acesso e na tela de contrato, ve o PDF GERADO integral com todas as infos corretas ja preenchidas (nome, CPF, plano, valor, vigencia, dados do profissional), rolavel, com Baixar; o resumo do plano acima do PDF usa os mesmos dados; o aceite com conferencia de CPF segue identico. Testar o fluxo completo: gestor cria contrato a partir de modelo, aluno abre o gate, le o PDF preenchido e assina com CPF. git add . && git commit -m "feat(aluno): gate exibe o PDF gerado com dados completos" && git push origin main

== WIDGETS (nao rodaram) ==
[ ] Enriquecer o card TREINOS NA SEMANA do Inicio com as bolinhas dos dias: ao lado do numero X de Y, uma linha com 7 bolinhas (S T Q Q S S D); dia com treino concluido = bolinha dourada preenchida com check escuro; dia pendente = contorno cinza; dia sem treino programado = contorno apagado. Calcular do historico de conclusao e da agenda da semana. git add . && git commit -m "feat(widgets): bolinhas da semana no card de treinos" && git push origin main
[ ] Widget PROGRESSO DO TREINO DE HOJE no Inicio: quando houver treino do dia, card com nome do treino, X de Y series concluidas e barra de progresso dourada com porcentagem; atualiza conforme o aluno marca series; some quando nao ha treino hoje; quando ja concluiu, mostra Concluido com check verde. git add . && git commit -m "feat(widgets): progresso do treino de hoje" && git push origin main
[ ] Widget SEQUENCIA no Inicio: card com numero grande dourado de dias seguidos treinando, rotulo "dias seguidos" e icone de chama; calcular do historico (dias consecutivos com ao menos um treino concluido); se for zero, frase motivacional curta em vez do numero. git add . && git commit -m "feat(widgets): sequencia de dias treinando" && git push origin main
[ ] Widget PESO CORPORAL no Inicio: peso mais recente em numero grande, variacao do mes colorida (queda verde com seta para baixo, aumento vermelho com seta para cima, estavel cinza) e mini grafico de linha SVG no padrao rpt ja usado no Progresso, com os pesos das atualizacoes enviadas. Estado vazio "Envie seu peso no Progresso para acompanhar". git add . && git commit -m "feat(widgets): peso corporal com variacao e mini grafico" && git push origin main
[ ] Widget ADESAO DO MES no Inicio: anel de progresso dourado em SVG com a porcentagem de treinos do mes concluidos e o texto X de Y treinos do mes; mesmo calculo de adesao que o gestor ve; estado vazio coerente sem treinos programados. git add . && git commit -m "feat(widgets): anel de adesao do mes" && git push origin main
[ ] Enriquecer o card PROXIMA ATIVIDADE do Inicio: faixa lateral colorida por tipo (Treino verde, Avaliacao #8B5CF6, Atualizacao #3B82F6, Retorno #F97316, Contrato dourado), nome, dia e hora, e botao pequeno Abrir que leva ao treino ou a agenda. Estado vazio "Nenhuma atividade agendada". git add . && git commit -m "feat(widgets): proxima atividade com tipo colorido e atalho" && git push origin main

== FECHAMENTO ==
[ ] Revisao final: ordenar o Inicio como saudacao, banner de treino em andamento (se houver), Progresso do treino de hoje, grade de metricas (Treinos na semana com bolinhas, Volume recente, Proxima atividade, Contrato), depois Sequencia, Peso e Adesao; confirmar tema escuro dourado em todos (nada branco sobre dourado), estados vazios elegantes, atualizacao ao concluir series, contrato gerado abrindo correto no gate; nenhum texto cortado, sem glow. Atualizar AUDITORIA-ALUNO.md. git add . && git commit -m "chore(aluno): revisao final de inicio, widgets e contrato gerado" && git push origin main
